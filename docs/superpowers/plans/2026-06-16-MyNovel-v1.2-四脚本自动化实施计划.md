# MyNovel v1.2 四脚本自动化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用测试驱动方式实现 MyNovel v1.2 的四个中文脚本：`小说文件.py`、`小说锚点.py`、`小说质检.py`、`小说上下文.py`。

**Architecture:** 四个中文脚本作为 Agent 调用入口，公共路径、章节解析、JSON 输出等逻辑放入 `公共工具.py`，量化词表和阈值放入 `质检规则.py`。测试使用 Python 标准库 `unittest`，先写失败测试，再实现最小代码让测试通过。

**Tech Stack:** Python 3.9 标准库、PowerShell、unittest、Markdown、Git。

---

## 文件结构

- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\公共工具.py`
  - 共享 UTF-8 读写、JSON 输出、章节号解析、章节范围解析、50 章归档段计算、项目目录别名识别、正文章节文件名解析。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\质检规则.py`
  - 存放 AI味三梯队词表、阈值、时间锚候选词、能力/资源候选词、默认禁用词。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\小说文件.py`
  - CLI：`scan-projects`、`chapter-status`、`archive-path`。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\小说质检.py`
  - CLI：`lint`。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\小说锚点.py`
  - CLI：`check`、`replace`。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\scripts\小说上下文.py`
  - CLI：`build`。
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\tests\test_scripts.py`
  - 22 个测试用例，覆盖 v1.2 设计文档中的 T01-T22。
- Create fixture files under: `C:\Users\Lenovo\.claude\skills\MyNovel\tests\fixtures\迷你小说项目\`
  - 迷你小说项目目录，用于测试项目扫描、同章多文件、锚点、上下文清单。
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿审查提示词.md`
  - 删除旧脚本目录引用，要求使用 `scripts/小说质检.py` 输出。
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿自动修复提示词.md`
  - 删除旧脚本目录引用，修复复查时引用 `scripts/小说质检.py`。
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\references\自动化脚本契约.md`
  - 从计划状态更新为 v1.2 已实现接口说明。
- Modify if needed: `C:\Users\Lenovo\.claude\skills\MyNovel\references\草稿审查流程.md`
  - 明确审查阶段脚本调用顺序。
- Modify if needed: `C:\Users\Lenovo\.claude\skills\MyNovel\references\草稿修复流程.md`
  - 明确修复后局部复查脚本调用。

## 执行规则

- 不使用第三方依赖。
- 不使用 `pytest`。
- 每个行为先写测试并确认失败。
- 每次实现后运行对应 `unittest` 类确认通过。
- 写入类操作必须验证 `--dry-run` 不修改文件。
- 每个任务完成后提交一次。

### Task 1: 创建测试夹具和测试骨架

**Files:**
- Create: `tests/test_scripts.py`
- Create: `tests/fixtures/迷你小说项目/novel-config.md`
- Create: `tests/fixtures/迷你小说项目/正文/第001章_开局.md`
- Create: `tests/fixtures/迷你小说项目/正文/第002章_冲突（草稿）.md`
- Create: `tests/fixtures/迷你小说项目/正文/第002章_冲突（已审查）.md`
- Create: `tests/fixtures/迷你小说项目/plan/大纲_第1-50章.md`
- Create: `tests/fixtures/迷你小说项目/章节总结/章节总结_第1-50章.md`
- Create: `tests/fixtures/迷你小说项目/阶段总结/阶段1_开局_1-10章.md`
- Create: `tests/fixtures/迷你小说项目/审查报告/审查_第1-50章.md`
- Create: `tests/fixtures/禁用词.txt`
- Create: `tests/fixtures/新内容.md`

- [ ] **Step 1: 写测试夹具**

Create fixture contents:

`novel-config.md`:

```markdown
# 小说配置

## 基本信息

- 书名：迷你小说项目
- 类型：测试

## 阶段划分

- 阶段1：开局（第1-10章）
```

`正文/第001章_开局.md`:

```markdown
# 第001章 开局

今天，主角获得系统。
他 A1，	好。
二十分钟后，他在晚上赶到。
系统解锁新能力，消耗10点积分。
不由得一愣，不由得停下。
震惊，不可思议，面色大变，空气凝固。
微微一笑，淡淡开口，缓缓转身，默默点头，轻轻抬手，深深看了他一眼。
绝对不能写。
```

`正文/第002章_冲突（草稿）.md`:

```markdown
# 第002章 冲突

这是第2章草稿。
```

`正文/第002章_冲突（已审查）.md`:

```markdown
# 第002章 冲突

这是第2章已审查版本。
```

`plan/大纲_第1-50章.md`:

```markdown
<!-- chapter:1 outline:start -->
# 第1章 大纲
开局。
<!-- chapter:1 outline:end -->

<!-- chapter:2 outline:start -->
# 第2章 大纲
冲突。
<!-- chapter:2 outline:end -->

<!-- chapter:3 outline:start -->
# 第3章 大纲
推进。
<!-- chapter:3 outline:end -->

<!-- chapter:4 outline:start -->
# 第4章 大纲
边界一。
<!-- chapter:4 outline:end -->

<!-- chapter:5 outline:start -->
# 第5章 大纲
边界二。
<!-- chapter:5 outline:end -->
```

`章节总结/章节总结_第1-50章.md`:

```markdown
<!-- chapter:1 summary:start -->
# 第1章 总结
原总结。
<!-- chapter:1 summary:end -->

<!-- chapter:2 summary:start -->
# 第2章 总结 A
重复锚点 A。
<!-- chapter:2 summary:end -->

<!-- chapter:2 summary:start -->
# 第2章 总结 B
重复锚点 B。
<!-- chapter:2 summary:end -->
```

`阶段总结/阶段1_开局_1-10章.md`:

```markdown
# 阶段总结：开局（第1-10章）

## 关键时间锚

- 第1章：今天主角获得系统。
```

`审查报告/审查_第1-50章.md`:

```markdown
# 审查_第1-50章
```

`禁用词.txt`:

```text
绝对不能写
```

`新内容.md`:

```markdown
# 第1章 总结
替换后的总结。
```

- [ ] **Step 2: 写测试骨架和 22 个测试名**

`tests/test_scripts.py` must define:

```python
import json
import subprocess
import sys
import unittest
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
SCRIPTS = ROOT / "scripts"
FIXTURES = ROOT / "tests" / "fixtures"
PROJECT = FIXTURES / "迷你小说项目"


def run_script(script_name, *args):
    cmd = [sys.executable, str(SCRIPTS / script_name)] + list(args)
    completed = subprocess.run(
        cmd,
        cwd=str(ROOT),
        text=True,
        encoding="utf-8",
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
    )
    if completed.returncode != 0:
        raise AssertionError(
            f"Command failed: {' '.join(cmd)}\nSTDOUT:\n{completed.stdout}\nSTDERR:\n{completed.stderr}"
        )
    return json.loads(completed.stdout)


class TestNovelFiles(unittest.TestCase):
    def test_scan_projects_finds_fixture_project(self):
        data = run_script("小说文件.py", "scan-projects", "--root", str(FIXTURES))
        self.assertTrue(data["ok"])
        names = [item["name"] for item in data["projects"]]
        self.assertIn("迷你小说项目", names)

    def test_scan_projects_returns_fuzzy_match_score(self):
        data = run_script("小说文件.py", "scan-projects", "--root", str(FIXTURES), "--query", "迷你")
        self.assertTrue(data["ok"])
        self.assertGreater(data["projects"][0]["match_score"], 0)
        self.assertTrue(data["projects"][0]["reason"])

    def test_chapter_status_parses_range_1_to_30(self):
        data = run_script("小说文件.py", "chapter-status", "--project", str(PROJECT), "--range", "1-30")
        self.assertEqual(data["range"], {"start": 1, "end": 30, "count": 30})

    def test_chapter_status_detects_duplicate_chapter_files(self):
        data = run_script("小说文件.py", "chapter-status", "--project", str(PROJECT), "--range", "1-30")
        duplicates = {item["chapter"]: item for item in data["duplicate_chapters"]}
        self.assertIn(2, duplicates)
        self.assertEqual(len(duplicates[2]["files"]), 2)

    def test_archive_path_calculates_51_to_100_for_chapter_61(self):
        data = run_script("小说文件.py", "archive-path", "--project", str(PROJECT), "--chapter", "61", "--type", "summary")
        self.assertEqual(data["archive"]["start"], 51)
        self.assertEqual(data["archive"]["end"], 100)
        self.assertIn("第51-100章", data["archive"]["path"])


class TestNovelLint(unittest.TestCase):
    def test_word_count_ignores_whitespace(self):
        text_file = FIXTURES / "word_count_sample.md"
        text_file.write_text("他 A1，\n\t好。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        self.assertEqual(data["word_count"]["count"], 6)

    def test_word_count_counts_letters_digits_and_punctuation(self):
        text_file = FIXTURES / "word_count_ascii_sample.md"
        text_file.write_text("A计划3分钟。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        self.assertEqual(data["word_count"]["count"], 7)

    def test_tier1_ai_flavor_triggers_per_term_at_two(self):
        text_file = FIXTURES / "tier1.md"
        text_file.write_text("他不由得停下，又不由得回头。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        self.assertTrue(data["ai_flavor"]["tier1"]["triggered"])
        self.assertEqual(data["ai_flavor"]["tier1"]["terms"]["不由得"]["count"], 2)

    def test_tier2_ai_flavor_triggers_by_total_above_three(self):
        text_file = FIXTURES / "tier2.md"
        text_file.write_text("震惊。不可思议。面色大变。空气凝固。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        self.assertEqual(data["ai_flavor"]["tier2"]["total"], 4)
        self.assertTrue(data["ai_flavor"]["tier2"]["triggered"])

    def test_tier3_ai_flavor_triggers_by_total_above_five(self):
        text_file = FIXTURES / "tier3.md"
        text_file.write_text("微微淡淡缓缓默默轻轻深深", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        self.assertEqual(data["ai_flavor"]["tier3"]["total"], 6)
        self.assertTrue(data["ai_flavor"]["tier3"]["triggered"])

    def test_extracts_time_anchor_candidates(self):
        text_file = FIXTURES / "time_anchor.md"
        text_file.write_text("二十分钟后，他在晚上赶到。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        first = data["time_anchor_candidates"][0]
        self.assertIn("二十分钟", first["matched_terms"])
        self.assertIn("晚上", first["matched_terms"])

    def test_extracts_ability_candidates(self):
        text_file = FIXTURES / "ability.md"
        text_file.write_text("系统解锁新能力，消耗10点积分。", encoding="utf-8")
        data = run_script("小说质检.py", "lint", "--file", str(text_file))
        first = data["ability_candidates"][0]
        for term in ["解锁", "能力", "消耗", "积分"]:
            self.assertIn(term, first["matched_terms"])

    def test_scans_forbidden_words_file(self):
        data = run_script(
            "小说质检.py",
            "lint",
            "--file",
            str(PROJECT / "正文" / "第001章_开局.md"),
            "--forbidden-words",
            str(FIXTURES / "禁用词.txt"),
        )
        matches = {item["word"]: item["count"] for item in data["forbidden_words"]["matches"]}
        self.assertEqual(matches["绝对不能写"], 1)


class TestNovelAnchor(unittest.TestCase):
    def test_check_existing_anchor_block(self):
        target = PROJECT / "章节总结" / "章节总结_第1-50章.md"
        data = run_script("小说锚点.py", "check", "--file", str(target), "--chapter", "1", "--kind", "summary")
        self.assertTrue(data["exists"])
        self.assertFalse(data["duplicate"])

    def test_check_duplicate_anchor_block(self):
        target = PROJECT / "章节总结" / "章节总结_第1-50章.md"
        data = run_script("小说锚点.py", "check", "--file", str(target), "--chapter", "2", "--kind", "summary")
        self.assertTrue(data["duplicate"])
        self.assertGreater(data["start_count"], 1)

    def test_check_missing_anchor_block(self):
        target = PROJECT / "章节总结" / "章节总结_第1-50章.md"
        data = run_script("小说锚点.py", "check", "--file", str(target), "--chapter", "9", "--kind", "summary")
        self.assertFalse(data["exists"])
        self.assertTrue(data["missing"])

    def test_replace_dry_run_does_not_modify_file(self):
        target = PROJECT / "章节总结" / "章节总结_第1-50章.md"
        before = target.read_text(encoding="utf-8")
        data = run_script(
            "小说锚点.py",
            "replace",
            "--file",
            str(target),
            "--chapter",
            "1",
            "--kind",
            "summary",
            "--content-file",
            str(FIXTURES / "新内容.md"),
            "--dry-run",
        )
        after = target.read_text(encoding="utf-8")
        self.assertTrue(data["dry_run"])
        self.assertEqual(before, after)

    def test_replace_changes_only_target_block(self):
        target = PROJECT / "章节总结" / "章节总结_第1-50章.md"
        original = target.read_text(encoding="utf-8")
        try:
            data = run_script(
                "小说锚点.py",
                "replace",
                "--file",
                str(target),
                "--chapter",
                "1",
                "--kind",
                "summary",
                "--content-file",
                str(FIXTURES / "新内容.md"),
            )
            changed = target.read_text(encoding="utf-8")
            self.assertTrue(data["changed"])
            self.assertIn("替换后的总结", changed)
            self.assertIn("重复锚点 A", changed)
            self.assertIn("重复锚点 B", changed)
        finally:
            target.write_text(original, encoding="utf-8")


class TestNovelContext(unittest.TestCase):
    def test_build_review_context_lists_required_files(self):
        data = run_script("小说上下文.py", "build", "--project", str(PROJECT), "--chapter", "3", "--mode", "review")
        self.assertTrue(data["ok"])
        self.assertTrue(data["novel_config"].endswith("novel-config.md"))
        self.assertTrue(data["chapter_summaries"])
        self.assertTrue(data["outline"]["current"])
        self.assertTrue(data["stage_summaries"])

    def test_recent_three_chapters_excludes_current_chapter(self):
        data = run_script("小说上下文.py", "build", "--project", str(PROJECT), "--chapter", "3", "--mode", "review")
        chapters = [item["chapter"] for item in data["recent_chapters"]]
        self.assertEqual(chapters, [1, 2])

    def test_recent_twenty_summary_points_to_archive_for_chapter_23(self):
        data = run_script("小说上下文.py", "build", "--project", str(PROJECT), "--chapter", "23", "--mode", "review")
        self.assertIn("章节总结_第1-50章.md", data["chapter_summaries"][0]["path"])
        self.assertEqual(data["chapter_summaries"][0]["range"]["start"], 3)
        self.assertEqual(data["chapter_summaries"][0]["range"]["end"], 22)

    def test_next_two_outline_boundary_chapters(self):
        data = run_script("小说上下文.py", "build", "--project", str(PROJECT), "--chapter", "3", "--mode", "review")
        self.assertEqual(data["outline"]["next_boundary_chapters"], [4, 5])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: 运行测试确认红灯**

Run:

```powershell
python -m unittest discover -s tests -v
```

Expected: tests fail because `scripts/小说文件.py`、`scripts/小说质检.py`、`scripts/小说锚点.py`、`scripts/小说上下文.py` do not exist yet.

- [ ] **Step 4: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- tests
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'test: add MyNovel v1.2 script fixtures'
```

Expected: commit includes only test files and fixtures.

### Task 2: 实现公共工具与质检规则

**Files:**
- Create: `scripts/公共工具.py`
- Create: `scripts/质检规则.py`

- [ ] **Step 1: 确认当前测试仍为红灯**

Run:

```powershell
python -m unittest discover -s tests -v
```

Expected: failures still point to missing script entry files.

- [ ] **Step 2: 实现 `公共工具.py`**

Required functions:

- `read_text(path)`
- `write_text(path, text)`
- `json_dump(data)`
- `success(command, **kwargs)`
- `failure(command, code, message)`
- `parse_chapter_number(text)`
- `parse_chapter_range(text)`
- `archive_span(chapter)`
- `find_first_existing_dir(project, aliases)`
- `parse_chapter_file(path)`
- `list_chapter_files(project)`

Behavior requirements:

- `parse_chapter_range("1-30")` returns `{"start": 1, "end": 30, "count": 30}`.
- `archive_span(61)` returns `(51, 100)`.
- `parse_chapter_file("第002章_冲突（草稿）.md")` returns chapter `2` and status `草稿`.
- UTF-8 is used for all reads/writes.

- [ ] **Step 3: 实现 `质检规则.py`**

Must define:

- `AI_TIER1`
- `AI_TIER2`
- `AI_TIER3`
- `AI_THRESHOLDS`
- `TIME_ANCHOR_TERMS`
- `ABILITY_TERMS`
- `DEFAULT_FORBIDDEN_WORDS`

- [ ] **Step 4: Run tests**

Run:

```powershell
python -m unittest discover -s tests -v
```

Expected: failures continue because the four entry scripts are still missing; no syntax errors from `公共工具.py` or `质检规则.py`.

- [ ] **Step 5: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts/公共工具.py scripts/质检规则.py
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'feat: add MyNovel script shared utilities'
```

Expected: commit succeeds.

### Task 3: TDD 实现 `小说文件.py`

**Files:**
- Create: `scripts/小说文件.py`
- Test: `tests/test_scripts.py::TestNovelFiles`

- [ ] **Step 1: Run file tests red**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelFiles -v
```

Expected: fails because `scripts/小说文件.py` does not exist.

- [ ] **Step 2: Implement minimal `小说文件.py`**

Required CLI:

- `scan-projects --root <path> [--query <text>] [--format json|markdown]`
- `chapter-status --project <path> --range <range> [--format json|markdown]`
- `archive-path --project <path> --chapter <n> --type summary|outline|review|repair [--format json|markdown]`

JSON requirements:

- `scan-projects` returns `{"ok": true, "command": "scan-projects", "projects": [...]}`.
- `chapter-status` returns `range`, `existing_chapters`, `missing_chapters`, `duplicate_chapters`, `highest_chapter`.
- `archive-path` returns `archive.start`, `archive.end`, `archive.path`, `archive.type`.

Directory aliases:

- summary: `章节总结`, `总结`
- outline: `plan`, `大纲`
- review: `审查报告`
- repair: `修复记录`

- [ ] **Step 3: Run file tests green**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelFiles -v
```

Expected: all 5 tests pass.

- [ ] **Step 4: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts/小说文件.py scripts/公共工具.py tests/test_scripts.py
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'feat: add MyNovel file discovery script'
```

Expected: commit succeeds.

### Task 4: TDD 实现 `小说质检.py`

**Files:**
- Create: `scripts/小说质检.py`
- Modify if needed: `scripts/质检规则.py`
- Modify if needed: `scripts/公共工具.py`
- Test: `tests/test_scripts.py::TestNovelLint`

- [ ] **Step 1: Run lint tests red**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelLint -v
```

Expected: fails because `scripts/小说质检.py` does not exist.

- [ ] **Step 2: Implement minimal `小说质检.py`**

Required CLI:

- `lint --file <path> [--forbidden-words <path>] [--format json|markdown]`

Required behavior:

- Word count is count of all non-whitespace characters.
- Tier 1 triggers when any single term count is `>= 2`.
- Tier 2 triggers when total count is `> 3`.
- Tier 3 triggers when total count is `> 5`.
- Time anchor candidates return sentence text and matched terms.
- Ability candidates return sentence text and matched terms.
- Forbidden words return matched word and count.

JSON keys:

- `ok`
- `command`
- `inputs`
- `word_count`
- `ai_flavor`
- `forbidden_words`
- `time_anchor_candidates`
- `ability_candidates`
- `warnings`

- [ ] **Step 3: Run lint tests green**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelLint -v
```

Expected: all 8 lint tests pass.

- [ ] **Step 4: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts/小说质检.py scripts/质检规则.py scripts/公共工具.py tests/test_scripts.py
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'feat: add MyNovel lint script'
```

Expected: commit succeeds.

### Task 5: TDD 实现 `小说锚点.py`

**Files:**
- Create: `scripts/小说锚点.py`
- Modify if needed: `scripts/公共工具.py`
- Test: `tests/test_scripts.py::TestNovelAnchor`

- [ ] **Step 1: Run anchor tests red**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelAnchor -v
```

Expected: fails because `scripts/小说锚点.py` does not exist.

- [ ] **Step 2: Implement minimal `小说锚点.py`**

Required CLI:

- `check --file <path> --chapter <n> --kind summary|review|outline|repair [--format json|markdown]`
- `replace --file <path> --chapter <n> --kind summary|review|outline|repair --content-file <path> [--dry-run] [--format json|markdown]`

Required behavior:

- Anchor format: `<!-- chapter:X kind:start -->` and `<!-- chapter:X kind:end -->`.
- `check` returns `exists`, `missing`, `duplicate`, `start_count`, `end_count`, line range if unique.
- `replace --dry-run` must not modify the file.
- Non-dry-run replacement changes only the target block content.
- If duplicate anchors exist for replacement target, return `ok=false` and do not write.

- [ ] **Step 3: Run anchor tests green**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelAnchor -v
```

Expected: all 5 anchor tests pass.

- [ ] **Step 4: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts/小说锚点.py scripts/公共工具.py tests/test_scripts.py
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'feat: add MyNovel anchor script'
```

Expected: commit succeeds.

### Task 6: TDD 实现 `小说上下文.py`

**Files:**
- Create: `scripts/小说上下文.py`
- Modify if needed: `scripts/公共工具.py`
- Test: `tests/test_scripts.py::TestNovelContext`

- [ ] **Step 1: Run context tests red**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelContext -v
```

Expected: fails because `scripts/小说上下文.py` does not exist.

- [ ] **Step 2: Implement minimal `小说上下文.py`**

Required CLI:

- `build --project <path> --chapter <n> --mode draft|review|repair [--format json|markdown]`

Required behavior:

- Return `novel_config`.
- Return previous up to 3 chapter files, excluding current chapter.
- Return chapter summary archive path and range from max(1, chapter - 20) to chapter - 1.
- Return current outline archive path.
- Return next boundary chapters `[chapter + 1, chapter + 2]`.
- Return stage summary candidates from `阶段总结` directory.

JSON keys:

- `ok`
- `command`
- `project`
- `chapter`
- `mode`
- `novel_config`
- `recent_chapters`
- `chapter_summaries`
- `outline`
- `stage_summaries`
- `foreshadowing_hints`

- [ ] **Step 3: Run context tests green**

Run:

```powershell
python -m unittest tests.test_scripts.TestNovelContext -v
```

Expected: all 4 context tests pass.

- [ ] **Step 4: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts/小说上下文.py scripts/公共工具.py tests/test_scripts.py
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'feat: add MyNovel context script'
```

Expected: commit succeeds.

### Task 7: 全量测试和真实项目冒烟测试

**Files:**
- Verify: `scripts/*.py`
- Verify: `tests/test_scripts.py`

- [ ] **Step 1: Run full unit tests**

Run:

```powershell
python -m unittest discover -s tests -v
```

Expected: all tests pass with final line containing `OK`.

- [ ] **Step 2: Run real project scan smoke test**

Run:

```powershell
python scripts/小说文件.py scan-projects --root D:\ai小说 --query 大医无双
```

Expected: JSON contains a project whose path includes `都市-大医无双`.

- [ ] **Step 3: Run real chapter status smoke test**

Run:

```powershell
python scripts/小说文件.py chapter-status --project D:\ai小说\小说\都市-大医无双 --range 1-30
```

Expected: JSON `ok=true`; `range.count=30`; output includes existing chapter data.

- [ ] **Step 4: Run real lint smoke test**

Run:

```powershell
python scripts/小说质检.py lint --file D:\ai小说\小说\都市-大医无双\正文\第001章_绝境.md
```

Expected: JSON `ok=true`; output includes `word_count` and `ai_flavor`.

- [ ] **Step 5: Run real context smoke test**

Run:

```powershell
python scripts/小说上下文.py build --project D:\ai小说\小说\都市-大医无双 --chapter 23 --mode review
```

Expected: JSON `ok=true`; output includes `novel_config`, `recent_chapters`, `chapter_summaries`, `outline`, and `stage_summaries`.

- [ ] **Step 6: Commit fixes if needed**

If smoke tests require small compatibility fixes:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- scripts tests
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'fix: harden MyNovel scripts against real project layout'
```

Expected: commit only includes script/test compatibility fixes.

### Task 8: 更新提示词和 references

**Files:**
- Modify: `prompts/草稿审查提示词.md`
- Modify: `prompts/草稿自动修复提示词.md`
- Modify: `references/自动化脚本契约.md`
- Modify if needed: `references/草稿审查流程.md`
- Modify if needed: `references/草稿修复流程.md`

- [ ] **Step 1: Remove old script directory references**

Search:

```powershell
$oldUpper = 'D:' + '\ai小说\提示词和脚本'
$oldLower = 'd:' + '\ai小说\提示词和脚本'
Select-String -Path 'prompts\*.md','references\*.md','SKILL.md' -Pattern $oldUpper,$oldLower -SimpleMatch
```

Expected before edit: existing hits in审查/修复提示词 for the old workspace script directory.

Edit so prompts refer to:

```text
C:\Users\Lenovo\.claude\skills\MyNovel\scripts\小说质检.py
```

- [ ] **Step 2: Compress quantitative prompt rules**

In `草稿审查提示词.md`:

- Replace detailed AI味三梯队 list with instruction to run `小说质检.py lint`.
- Keep A/B problem classes, literary judgment, evidence requirements, and report format.
- Keep rule: pure review does not modify正文.

In `草稿自动修复提示词.md`:

- Replace repeated AI味 thresholds with instruction to use `小说质检.py` before/after repair.
- Keep repair policy and examples for local rewrite.

- [ ] **Step 3: Update script contract**

In `references/自动化脚本契约.md`, update first-round status to v1.2 implemented status:

- list each command
- show one PowerShell example per script
- explain JSON output is authoritative evidence

- [ ] **Step 4: Verify no old paths remain**

Run:

```powershell
$oldUpper = 'D:' + '\ai小说\提示词和脚本'
$oldLower = 'd:' + '\ai小说\提示词和脚本'
Select-String -Path 'prompts\*.md','references\*.md','SKILL.md' -Pattern $oldUpper,$oldLower -SimpleMatch
```

Expected: no output.

- [ ] **Step 5: Recount prompt size**

Run a PowerShell count equivalent to the desktop baseline:

```powershell
$promptDir = 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts'
$items = Get-ChildItem -LiteralPath $promptDir -File | Sort-Object Name | ForEach-Object {
  $text = Get-Content -LiteralPath $_.FullName -Raw -Encoding UTF8
  $lines = (Get-Content -LiteralPath $_.FullName -Encoding UTF8).Count
  $chars = $text.Length
  $nonWhitespace = ([regex]::Replace($text, '\s', '')).Length
  [PSCustomObject]@{ Name = $_.Name; Lines = $lines; Chars = $chars; NonWhitespaceChars = $nonWhitespace }
}
$items | Format-Table -AutoSize
($items | Measure-Object Lines,Chars,NonWhitespaceChars -Sum)
```

Expected: output is used to update desktop baseline file.

- [ ] **Step 6: Update desktop comparison file**

Append script化后数据 to:

```text
C:\Users\Lenovo\Desktop\MyNovel提示词字数基线.txt
```

Use the same fields:

- total files
- total lines
- total chars
- total non-whitespace chars
- before/after delta

- [ ] **Step 7: Commit**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- prompts references
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m 'docs: route MyNovel prompts through v1.2 scripts'
```

Expected: commit includes prompt/reference updates. Desktop txt is outside the repo and is not committed.

### Task 9: 最终验证和标签

**Files:**
- Verify all v1.2 files.

- [ ] **Step 1: Run full tests**

Run:

```powershell
python -m unittest discover -s tests -v
```

Expected: all tests pass, final line contains `OK`.

- [ ] **Step 2: Verify Python 3.9 compatibility**

Run:

```powershell
python --version
python -m unittest discover -s tests -v
```

Expected: Python reports `3.9.13`; tests pass.

- [ ] **Step 3: Verify no old paths or English script names remain**

Run:

```powershell
$oldUpper = 'D:' + '\ai小说\提示词和脚本'
$oldLower = 'd:' + '\ai小说\提示词和脚本'
$oldNames = @(
  ('novel' + '_lint.py'),
  ('novel' + '_files.py'),
  ('novel' + '_anchor.py'),
  ('novel' + '_context.py')
)
$patterns = @($oldUpper, $oldLower) + $oldNames
Select-String -Path 'SKILL.md','references\*.md','prompts\*.md','docs\superpowers\specs\*.md','docs\superpowers\plans\*.md' -Pattern $patterns -SimpleMatch
```

Expected: no output, except historical docs may be ignored only if they are pre-v1.2 design archives. If historical docs produce hits, add a note in final report instead of rewriting history.

- [ ] **Step 4: Verify git status**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' status --short
```

Expected: no output.

- [ ] **Step 5: Create v1.2 tag**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' tag -a v1.2 -m 'MyNovel skill v1.2: add four Chinese automation scripts with TDD tests'
```

Expected: tag created.

- [ ] **Step 6: Final report**

Report:

- four script files created
- shared modules created
- tests pass
- real project smoke tests pass
- prompt before/after count
- v1.2 tag

## 自检

- Spec coverage: Tasks cover four scripts, shared modules, fixtures, TDD tests, prompt compression, old-path removal, smoke tests, and v1.2 tag.
- Test coverage: The plan maps to T01-T22 from the v1.2 design.
- TDD compliance: Each script task starts by running the relevant test class red before implementation.
- Dependency policy: Only Python standard library is used.
- Risk: Task 8 prompt compression may reduce token counts but must not remove literary judgment rules; keep A/B classes, evidence rules, and output format.
