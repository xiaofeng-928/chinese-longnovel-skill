# MyNovel v1.3 开篇与章末钩子实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把开篇设计、章末钩子设计、修改方案推荐逻辑接入 MyNovel v1.3。

**Architecture:** 不新增独立开头/钩子技法库。开篇和章末钩子规则融入现有提示词；新增一个共享提示词 `prompts/修改建议与技法应用提示词.md`，只处理“怎么改”和高风险修复方案选择。

**Tech Stack:** Markdown prompt files, PowerShell verification commands, existing Python unittest suite.

---

## 文件结构

- Modify: `SKILL.md`
  - 更新路由表、参考提示词列表，加入“修改建议”意图。
- Create: `prompts/修改建议与技法应用提示词.md`
  - 定义修改方案推荐、低风险/高风险判断、方案输出格式。
- Modify: `prompts/分阶段大纲细化提示词.md`
  - 新增“开篇设计”字段，扩展章末钩子类型。
- Modify: `prompts/黄金三章微操细纲提示词.md`
  - 给前三章微操细纲补齐“开篇设计”和结构化章末钩子字段。
- Modify: `prompts/草稿生成提示词.md`
  - 让草稿生成提取并执行开篇设计和章末钩子类型。
- Modify: `prompts/草稿审查提示词.md`
  - 新增开篇承接、开篇牵引、章末钩子有效性、钩子重复检查。
- Modify: `prompts/草稿自动修复提示词.md`
  - 加入“低风险直接修，高风险先给方案”的规则。
- Do not modify: `prompts/大纲生成提示词.md`
  - 总纲层不生成单章开篇/章末字段，本次保持不动。

---

### Task 1: 新增共享修改建议提示词

**Files:**
- Create: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\修改建议与技法应用提示词.md`

- [ ] **Step 1: 创建共享提示词文件**

Use `apply_patch` to create the file with this complete content:

```markdown
# 修改建议与技法应用提示词

> 使用说明：当用户明确询问“怎么改”“推荐怎么改”“给几个修改方案”，或草稿自动修复遇到高风险改动时调用。本提示词不直接替代草稿生成、草稿审查或草稿自动修复流程，只负责把问题转换成可选择的修改方案。

---

你是一位网文修稿编辑。你的任务不是直接重写全文，而是根据审查问题、章节细纲、已拼接上下文和后续边界，给出可执行的修改方案。

## 输入

- 书名：【用户填写】
- 章节范围：【如“第30章”或“第28-30章”】
- 待处理问题：【粘贴审查报告中的问题，或用户描述的问题】
- 对应章节细纲：【必须包含开篇设计、核心事件、爽点/钩子、章末钩子、与下一章衔接；没有则说明缺失】
- 已拼接上下文摘要：【包含前文硬事实、角色状态、信息流边界、伏笔边界、后续2章边界】
- 用户意图：【推荐方案 / 选择修法 / 你选最稳的方案直接改】

## 判断顺序

1. 先判断问题属于低风险小修还是高风险结构修。
2. 低风险小修可以直接给出推荐改法和示例句。
3. 高风险结构修必须给 2-3 个方案，不得直接替用户决定。
4. 如果用户明确说“你选最稳的方案直接改”，选择风险最低、最不改变事件顺序的方案。

## 低风险小修

以下情况属于低风险，可以直接建议或直接修：

- 把平淡的章末句改成更有牵引力的一句话，但不改变事件。
- 调整第一段的承接句，让它接住上一章状态，但不改变事件。
- 强化已有钩子句，但不改变章末钩子类型。
- 删除或替换几句解释性旁白，让开头更快进入行动。
- 在已有动作中补一句角色反应，让前20%更有压力或信息差。

## 高风险结构修

以下情况属于高风险，必须先给方案：

- 改变本章开篇方式。
- 重排开头场景。
- 新增细纲里没有的开篇动作、事件或信息。
- 改变章末钩子类型。
- 新增较大篇幅的结尾段落。
- 修改会影响节奏、事件顺序、角色认知、伏笔含义或后续两章边界。

## 方案输出格式

```markdown
## 修改方案

### 方案A：保守局部修
- 适用：
- 改动范围：
- 预期效果：
- 风险：
- 示例改法：

### 方案B：结构强化修
- 适用：
- 改动范围：
- 预期效果：
- 风险：
- 示例改法：

### 方案C：（仅在确有第三种清晰路径时输出）
- 适用：
- 改动范围：
- 预期效果：
- 风险：
- 示例改法：

## 推荐

推荐选择：【A/B/C】
理由：（一句话说明为什么最稳）
```

## 硬性限制

- 不得新增主线事件。
- 不得新增角色。
- 不得新增设定、能力、资源或伏笔。
- 不得提前消耗后续两章核心事件。
- 不得让角色知道其当前认知边界外的信息。
- 不得为了制造钩子改变细纲规定的章末落点。
- 方案必须能落回原章节正文，不允许变成新章节设计。
```

- [ ] **Step 2: 验证共享提示词文件存在**

Run:

```powershell
Test-Path -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\修改建议与技法应用提示词.md'
```

Expected:

```text
True
```

- [ ] **Step 3: Commit Task 1**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/修改建议与技法应用提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "feat: add MyNovel modification advice prompt"
```

---

### Task 2: 更新 `SKILL.md` 路由与提示词列表

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\SKILL.md`

- [ ] **Step 1: 在智能路由表加入修改建议行**

In `SKILL.md`, add this row near review/repair routes:

```markdown
| 推荐怎么改、给几个修改方案、这个问题怎么修 | 修改建议与技法应用 | `上下文组装.md`、`草稿审查流程.md`、`草稿修复流程.md` | `修改建议与技法应用提示词.md` |
```

- [ ] **Step 2: 在提示词列表加入新文件**

In the `## 提示词` list, add:

```markdown
- `prompts/修改建议与技法应用提示词.md`
```

- [ ] **Step 3: 在路由说明中补充条件读取规则**

After the route table, add this paragraph:

```markdown
`修改建议与技法应用提示词.md` 只在两种情况下读取：用户明确要求推荐修改方案；或草稿修复遇到会改变开篇方式、章末钩子类型、事件顺序、角色认知、伏笔含义、后续两章边界的高风险改动。普通低风险小修不强制读取。
```

- [ ] **Step 4: 验证路由和提示词列表**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\SKILL.md' -Pattern '修改建议与技法应用','推荐怎么改','给几个修改方案','这个问题怎么修'
```

Expected: output includes the new route row, the new prompt list item, and the conditional read paragraph.

- [ ] **Step 5: Commit Task 2**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'SKILL.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: route MyNovel modification advice prompt"
```

---

### Task 3: 更新分阶段大纲细化提示词

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\分阶段大纲细化提示词.md`

- [ ] **Step 1: 在每章大纲格式中加入开篇设计**

In the `### 每章大纲格式` block, add this section after chapter start state and before core event:

```markdown
**开篇设计**：
- 开篇方式：（从“行动中开场 / 反常情境 / 震撼对话 / 倒计时压力 / 重大发现 / 危机时刻 / 谜团浮现 / 背叛开场 / 重大选择 / 结局预告”中选择一种；如不适用，写“承接式开场”）
- 承接上一章：（说明如何接住上一章结尾钩子/状态；如为第1章，写“无，负责建立开篇承诺”）
- 前20%牵引：（异常、压力、冲突、信息差或行动目标）
```

- [ ] **Step 2: 强化爽点/钩子块**

Replace the current chapter hook lines:

```markdown
**爽点/钩子**：
- 爽点：（如有，如"当众施针患者站起""周子轩败北"）
- 章末钩子：（结尾留什么悬念驱动下一章）
```

with:

```markdown
**爽点/钩子**：
- 爽点：（如有，如"当众施针患者站起""周子轩败北"）
- 章末钩子类型：（从“突然揭示 / 紧急危机 / 未完成动作 / 身份或立场反转 / 两难选择 / 神秘线索 / 时间限制 / 承诺或威胁 / 言外之意 / 回声或呼应 / 留白钩子 / 升级预告 / 冲突预告”中选择一种）
- 章末钩子：（结尾留什么具体期待驱动下一章）
```

- [ ] **Step 3: 替换四类钩子约束**

Replace the existing constraint sentence that says hook types can be only:

```text
悬念、逆转、升级预告、冲突预告
```

with:

```markdown
1. **每章必须有章末钩子**——没有钩子的章节读者会弃书。章末钩子必须从本提示词给出的扩展类型中选择，不能只写“留下悬念”。短期内不要反复使用同一种钩子形态；如果必须回环，必须升级为更强变体。
```

- [ ] **Step 4: 更新轻量自检**

In the final self-check table, ensure the repetition row checks opening and hook shape:

```markdown
| 重复度：开篇方式、冲突手段、爽点/章末钩子是否短期撞车 | 通过/风险/需调整 | 对照历史细纲与冲突手段台账 | 如无问题写"无" |
```

- [ ] **Step 5: 验证分阶段大纲提示词**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\分阶段大纲细化提示词.md' -Pattern '开篇设计','开篇方式','前20%牵引','章末钩子类型','突然揭示','未完成动作','只有四类'
```

Expected: output includes the new fields and expanded hook types; it should not include any wording saying hook types are only four categories.

- [ ] **Step 6: Commit Task 3**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/分阶段大纲细化提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: add opening and hook fields to staged outlines"
```

---

### Task 4: 更新黄金三章微操细纲提示词

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\黄金三章微操细纲提示词.md`

- [ ] **Step 1: 在每章输出格式加入开篇设计**

In the output template, add this section after `**本章核心使命**`:

```markdown
### 开篇设计

- **开篇方式**：（行动中开场 / 反常情境 / 震撼对话 / 倒计时压力 / 重大发现 / 危机时刻 / 谜团浮现 / 背叛开场 / 重大选择 / 结局预告；第1章优先选择能立刻建立开篇承诺的方式）
- **承接上一章**：（第1章写“无，负责建立开篇承诺”；第2-3章必须说明如何接住上一章章末钩子）
- **前20%牵引**：（冲突、压迫、异常、信息差或行动目标）
```

- [ ] **Step 2: 结构化章末悬念**

Replace:

```markdown
### 章末悬念（钩子）

（精确描述章末最后一幕的画面+悬念点，让读者必须翻下一章）
```

with:

```markdown
### 章末悬念（钩子）

- **钩子类型**：（突然揭示 / 紧急危机 / 未完成动作 / 身份或立场反转 / 两难选择 / 神秘线索 / 时间限制 / 承诺或威胁 / 言外之意 / 回声或呼应 / 留白钩子 / 升级预告 / 冲突预告）
- **最后一幕画面**：（精确描述章末最后一幕的画面）
- **悬念点**：（说明读者为什么必须翻下一章）
```

- [ ] **Step 3: 更新状态检查表**

Add this row to `黄金三章状态检查`:

```markdown
| 开篇是否有牵引 | ✅/❌ | ✅/❌ | ✅/❌ |
```

Keep the existing `章末是否有钩子` row.

- [ ] **Step 4: 验证黄金三章提示词**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\黄金三章微操细纲提示词.md' -Pattern '开篇设计','承接上一章','前20%牵引','钩子类型','最后一幕画面','开篇是否有牵引'
```

Expected: output includes all six terms.

- [ ] **Step 5: Commit Task 4**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/黄金三章微操细纲提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: align golden three outline with opening hooks"
```

---

### Task 5: 更新草稿生成提示词

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿生成提示词.md`

- [ ] **Step 1: 扩展大纲硬要求提取项**

Replace the sentence that extracts outline hard requirements with:

```markdown
1. **先提取本章大纲硬要求**——章节号、章节名、起始状态、开篇方式、承接上一章、前20%牵引、必须发生的核心事件、出场角色、爽点/钩子、伏笔操作、章末钩子类型、章末钩子、与下一章衔接
```

- [ ] **Step 2: 在结构约束中加入开篇执行规则**

Add this item under `### 一、结构约束` before the current chapter-ending rule:

```markdown
2. **章节必须按开篇设计进入**——如果细纲提供了“开篇方式 / 承接上一章 / 前20%牵引”，正文前20%必须执行：接住上一章状态或钩子，尽快给出行动、压力、异常、信息差、冲突或情绪牵引。不得用天气、日常流程、复述上一章来开头，除非细纲明确要求且能形成压力。
```

If numbering becomes duplicated, renumber only the local list items in this section.

- [ ] **Step 3: 强化章末钩子执行规则**

Replace the existing chapter-ending rule with:

```markdown
3. **章节必须以指定章末钩子收束**——大纲中标注了章末钩子类型和具体钩子，就必须按该类型和钩子内容收束。不允许写一个平淡结尾后说“下章再说”，也不得为了制造钩子提前消耗后续2章边界中的核心事件。
```

- [ ] **Step 4: 验证草稿生成提示词**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿生成提示词.md' -Pattern '开篇方式','承接上一章','前20%牵引','章末钩子类型','按开篇设计进入','指定章末钩子'
```

Expected: output includes all six terms.

- [ ] **Step 5: Commit Task 5**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/草稿生成提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: enforce opening and hook fields in draft generation"
```

---

### Task 6: 更新草稿审查提示词

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿审查提示词.md`

- [ ] **Step 1: 在输入中要求完整细纲包含新字段**

Update the outline input description to require opening and hook fields:

```markdown
- 对应章节完整大纲：【粘贴分阶段大纲中对应章节的详细大纲；必须包含开篇设计、核心事件、出场角色、爽点/钩子、章末钩子类型、章末钩子、与下一章衔接】
```

- [ ] **Step 2: 在 B2 节奏问题中加入开篇检查**

Add these checks in `#### B2：节奏问题` before `章末无钩子`:

```markdown
22. **开篇承接失败**：本章开头是否没有接住上一章结尾状态/钩子，导致读者感觉断片？
23. **开篇牵引不足**：前20%是否缺少行动、压力、异常、信息差、冲突或情绪牵引，只是在复述、铺陈或日常流程？
24. **开篇偏离细纲**：正文开篇是否无视细纲中的开篇方式、承接上一章或前20%牵引？
25. **章末无钩子或钩子无效**：是否有章节以平淡结尾收束，或钩子没有形成具体的下一章期待？
26. **钩子短期重复**：近期章节是否反复使用同一种章末钩子形态，且没有升级或变体？
```

Renumber following B2/B3 items so the list stays sequential.

- [ ] **Step 3: 增加修改建议触发规则**

Add this rule under `## 审查原则`:

```markdown
8. **用户问怎么改时给方案**：如果用户明确问“怎么改”“推荐怎么改”“给几个修改方案”，必须读取 `修改建议与技法应用提示词.md`，把相关问题转成2-3个可选方案；不要只给一个唯一答案。
```

- [ ] **Step 4: 验证草稿审查提示词**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿审查提示词.md' -Pattern '开篇承接失败','开篇牵引不足','开篇偏离细纲','钩子无效','钩子短期重复','修改建议与技法应用提示词'
```

Expected: output includes all six terms.

- [ ] **Step 5: Commit Task 6**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/草稿审查提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: review opening and hook execution"
```

---

### Task 7: 更新草稿自动修复提示词

**Files:**
- Modify: `C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿自动修复提示词.md`

- [ ] **Step 1: 在输入中要求完整细纲包含新字段**

Update the outline input description to:

```markdown
- 对应章节完整大纲：【粘贴分阶段大纲中对应章节的详细大纲；多章修复时按章节分别粘贴；必须包含开篇设计、核心事件、爽点/钩子、章末钩子类型、章末钩子、与下一章衔接】
```

- [ ] **Step 2: 在修复判定规则中加入低/高风险规则**

Add this section after `### AI判断是否修复`:

```markdown
### v1.3 首尾修复风险分级

低风险修复可以直接做：
- 把平淡结尾改成更有牵引力的一句话，但不改变事件。
- 调整第一段，让它更自然承接上一章，但不改变事件。
- 强化已有钩子句，但不改变章末钩子类型。

高风险修复必须先给用户2-3个方案，并读取 `修改建议与技法应用提示词.md`：
- 改变本章开篇方式。
- 重排开头场景。
- 新增细纲里没有的开篇动作或信息。
- 改变章末钩子类型。
- 新增较大篇幅的结尾段落。
- 任何会影响节奏、事件顺序、角色认知、伏笔含义或后续两章边界的修复。
```

- [ ] **Step 3: 更新 B2 章末钩子修复规则**

Replace the current `B2章末钩子修复` line with:

```markdown
- B2章末钩子修复时，优先在原章末落点上强化一句具体钩子；如果需要改变章末钩子类型、增加新事件或改动后续衔接，必须先给用户2-3个方案。
```

- [ ] **Step 4: 增加开篇修复规则**

Add this line near B1/B2 repair rules:

```markdown
- B2开篇问题修复时，优先调整开头1-3段的承接句、压力信息或行动目标；如果需要改变开篇方式或重排场景，必须先给用户2-3个方案。
```

- [ ] **Step 5: 验证草稿自动修复提示词**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Select-String -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel\prompts\草稿自动修复提示词.md' -Pattern '首尾修复风险分级','低风险修复','高风险修复','修改建议与技法应用提示词','B2开篇问题','B2章末钩子修复'
```

Expected: output includes all six terms.

- [ ] **Step 6: Commit Task 7**

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' add -- 'prompts/草稿自动修复提示词.md'
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' commit -m "docs: add opening hook repair choice rules"
```

---

### Task 8: 全局回归检查

**Files:**
- Verify only; no planned modifications.

- [ ] **Step 1: 确认大纲生成提示词未修改**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' diff --name-only HEAD~6..HEAD | Select-String -Pattern '大纲生成提示词.md'
```

Expected: no output.

- [ ] **Step 2: 确认没有新增独立开头/钩子库**

Run:

```powershell
Get-ChildItem -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel' -Recurse -File | Where-Object { $_.Name -in @('单章开头技法.md','章末钩子库.md') } | Select-Object FullName
```

Expected: no output.

- [ ] **Step 3: 确认 v1.2 删除脚本没有被重新引用**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Get-ChildItem -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel' -Recurse -File -Include *.md | Select-String -Pattern '小说文件\.py','小说锚点\.py','小说上下文\.py' | Select-Object Path,LineNumber,Line
```

Expected: no output.

- [ ] **Step 4: 确认关键 v1.3 术语都已接入**

Run:

```powershell
[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new($false)
Get-ChildItem -LiteralPath 'C:\Users\Lenovo\.claude\skills\MyNovel' -Recurse -File -Include *.md | Select-String -Pattern '修改建议与技法应用提示词','开篇设计','章末钩子类型','前20%牵引','高风险修复' | Select-Object Path,LineNumber,Line
```

Expected: output includes `SKILL.md`, `分阶段大纲细化提示词.md`, `黄金三章微操细纲提示词.md`, `草稿生成提示词.md`, `草稿审查提示词.md`, `草稿自动修复提示词.md`, and `修改建议与技法应用提示词.md`.

- [ ] **Step 5: 运行脚本测试**

Run:

```powershell
py -3 -m unittest discover -s tests -v
```

Expected:

```text
Ran 11 tests
OK
```

- [ ] **Step 6: 查看最终状态**

Run:

```powershell
git -C 'C:\Users\Lenovo\.claude\skills\MyNovel' status --short --branch
```

Expected: branch may be ahead by multiple commits; no uncommitted changes.

