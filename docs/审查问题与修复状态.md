# MyNovel Skill 审查问题与修复状态

## 已修复

- 黄金三章文件名和第 1-50 章细纲前置规则不一致。
- `总结/`、`章节总结/`、`阶段总结/`目录契约不一致。
- 禁用词默认词表为空，导致审查无法判断配置缺失。
- Skill 名称不符合标准 hyphen-case 的发布规范。
- 自动修复的高风险确认流程与普通修复边界冲突。
- README 与实际资源、批次上限和流程名称过时。
- 上下文、总结、正文生成和修复文档中残留缓冲章/章内补强规则。
- 新增细纲整理路由，明确笼统构想必须先整理成正式细纲。
- 上下文改为按有效章节序列读取，并补读最近主线锚点。
- 已写出未审查正文仍作为有效历史正文进入上下文。

## 按用户要求不修复

- 没有独立细纲审查路由。
- 章内补强可以绕过完整审查。

## 当前设计约束

- `plan/` 中已写入的细纲默认是正式细纲，不存在细纲草稿状态。
- `第49(1)章` 等括号编号保持现有文件命名格式，由章节序列决定有效顺序。
- 支线标签不等于排除上下文；主线锚点单独决定主线回溯。
