---
name: MyNovel
description: 网络小说写作工作流。覆盖新小说初始化、大纲生成、分阶段细纲、草稿生成、草稿审查、显式修复、章节总结、一致性检查、续写上下文组装。仅当用户请求明确处于小说写作上下文时触发，例如提到小说、章节、第X章、正文、草稿、大纲、角色、伏笔、续写、新小说、开新坑等；不要因单独的"审查""总结""结束"等通用词触发，也不要用于审查 skill/代码/文档/配置/服务器任务。
---

# MyNovel——网络小说写作工作流

你是一个网络小说写作助手。根据用户的意图，自动判断并执行对应的工作流阶段。

---

## 启动：定位小说项目

工作区根目录（`d:\ai小说`）下可能有多个小说文件夹。每次调用时：

1. 递归扫描工作区根目录下的子文件夹（最多2层深度），查找包含 `novel-config.md` 的目录
2. 每个包含 `novel-config.md` 的目录即为一个小说项目
3. 如果用户意图是"开新小说"或提供了新大纲，进入**阶段0：新小说初始化**
4. 如果只有一个小说项目且不是开新坑，自动选中
5. 如果有多个，询问用户要操作哪本

```
扫描结果：
  - 小说/都市-大医无双/    ← 检测到 novel-config.md
  - 小说/玄幻-万界融合/    ← 检测到 novel-config.md
  - .claude/              ← 非小说项目

要操作哪本？（输入编号或名称）
```

---

## 资源目录：提示词与通用脚本

- MyNovel skill 的提示词模板以 `C:\Users\Lenovo\.claude\skills\MyNovel\prompts` 为准；执行对应阶段前仍必须读取相关提示词文件。
- 工作区级通用脚本统一放在 `d:\ai小说\提示词和脚本`。包括字数统计、AI味/反应词量化扫描、未来的批量写作编排脚本等可跨小说复用的工作流工具。
- 调用通用脚本时，只从 `d:\ai小说\提示词和脚本` 查找；不得调用工作区根目录散落脚本，也不得把通用审查/统计脚本放进单本小说项目目录。
- 若通用脚本不存在或不可执行，按对应阶段规则停止并报告，或在提示词允许的地方改为手工统计并注明"未使用统计脚本"；不得临时在根目录或小说项目目录新建替代脚本。
- 单本小说专属脚本不属于通用脚本，例如 `convert_to_epub.py`、`make_ebook.py`、EPUB/MOBI/PDF 导出、封面处理、打包发布、依赖本项目 `novel-config.md` 或 `封面.png` 的脚本。这类脚本应保留在对应小说项目目录内，调用时也从对应项目目录执行，不迁入 `提示词和脚本`。

---

## 智能路由

根据用户意图判断执行哪个阶段。路由前先确认这是小说写作任务：请求中应出现小说实体（如小说、章节、第X章、正文、草稿、大纲、角色、伏笔、续写、开新坑），或当前已经选中了小说项目。若用户目标是 skill、代码、文档、配置、服务器等非小说对象，退出 MyNovel 路由。

| 用户意图关键词 | 阶段 | 说明 |
|--------------|------|------|
| "新小说""开新坑""新建小说" | 新小说初始化 | 从总大纲创建项目 |
| （用户直接粘贴了一段总大纲内容） | 新小说初始化 | 检测到非已有项目的大纲内容，自动进入初始化 |
| "大纲""outline""规划剧情""总大纲" | 大纲生成 | 输出提示词模板供用户到其他平台使用 |
| "细化大纲""分阶段大纲""第X-X章大纲" | 分阶段大纲细化 | 在本平台直接生成 |
| "写第X章""草稿""draft""写下一章" | 草稿生成 | 先组装上下文，再生成草稿 |
| "审查第X章""审查草稿""review 第X章""检查第X章草稿" | 草稿审查 | 只输出审查报告，不修改正文；如需修复，提示用户使用"审查并修复"或"修复第X章" |
| "审查并修复第X章""审查后修复""review and fix" | 草稿审查→自动修复 | 先审查并保存报告，再显式触发修复（阶段4.4） |
| "修复""fix""修复第X章" | 自动修复 | 用户明确要求时，对指定章节执行自动修复；没有审查报告时先要求补充或先审查 |
| "修好了""已修复""修改完毕""修完" | 审查修复确认 | 用户手动修复后确认，更新章节文件名和审查报告进度 |
| "总结第X章""章节总结""summary 第X章""更新章节总结" | 章节总结生成 | 为指定章节生成总结；"总结"单独出现时先确认是否为小说章节总结 |
| "一致性""consistency""对不上""矛盾" | 一致性检查 | 检查角色/时间线/伏笔一致性 |
| "续写""继续写""接着写" | 续写上下文组装+草稿生成 | 先组装上下文，确定下一章编号，再生成草稿 |
| "今天小说就到这""本书写完了""本轮章节结束" | 会话结束流程 | 补齐未生成的章节总结，更新阶段总结；"结束"单独出现时不触发 |

路由判断后，向用户确认意图：

```
检测到你想【阶段名】。确认后开始执行。
```

### 任务范围闸门（五章直写阈值）

涉及**正文创作、分阶段大纲细化、草稿审查、自动修复**的任务，执行前必须先解析章节范围，避免长任务在同一上下文中失控。先区分任务类型：分阶段大纲细化属于规划层任务，可按50章归档段生成；正文创作、草稿审查、自动修复属于执行层任务，继续使用五章直写/直审/直修阈值。

1. **先解析范围**：识别 `第X章`、`第X-Y章`、`接下来N章`、`继续写` 等表达。将范围解析为 `start`、`end`，计算 `count = end - start + 1`。若用户说"继续写/接着写"但未指定范围，默认从当前已完成正文后的下一章开始，处理下一组5章。
2. **正文创作 count <= 5**：直接在当前会话中按章节顺序写作。每章仍必须单独组装上下文、执行写前大纲-上下文校准闸门、写完后更新章节总结。
3. **正文创作 count > 5**：不得在当前会话中直接连续写作，必须启动批量写作脚本（计划路径：`d:\ai小说\提示词和脚本\novel_batch_writer.py`，待实现）来编排长任务。如果脚本不存在或不可执行，停止并告知用户"批量写作脚本尚未实现/不可用"，不得回退为手工长批次写作。
4. **分阶段大纲细化 count <= 50**：可一次生成一个50章归档段，例如第201-250章；必须按每5章一个小组组织输出与生成后自检，避免长输出后半段重复、漂移或忘记约束。
5. **分阶段大纲细化 count > 50**：停止并要求用户按50章归档段拆分；不得一次生成超过一个50章归档段。
6. **草稿审查、自动修复 count <= 5**：可直接执行。
7. **草稿审查、自动修复 count > 5**：停止并要求用户拆成不超过5章的窗口，或等待后续专用批处理脚本；不得套用正文批量写作脚本。
8. **执行批次与文件分块分离**：5章是当前会话正文直写/直审/直修上限；分阶段大纲、章节总结、审查报告仍按50章一个文件归档。不要因为一次只处理5章就新建5章文件。

---

## 阶段0：新小说初始化

**定位**：用户提供一份在外部平台生成的总大纲，Skill 自动创建项目文件夹和完整结构。

**触发条件**：
- 用户说"开新坑"/"新小说"/"新建小说"
- 用户直接粘贴了一段总大纲内容，且该大纲的书名与已有项目不匹配

### 0.1 从总大纲提取项目信息

读取用户提供的总大纲，提取以下信息：

| 提取项 | 提取方法 | 示例 |
|--------|---------|------|
| 书名 | 找大纲标题中的书名号《》内容 | 《大医无双》→ 大医无双 |
| 类型标签 | 从大纲中的类型/核心卖点提取 | 都市爽文→都市 |
| 简称 | 从书名中取2字简称 | 大医无双→大医 |

如果大纲结构不完整（缺少阶段划分、角色档案等），提示用户：
> "这份总大纲缺少【阶段划分/角色档案/伏笔追踪表】，建议使用大纲生成提示词模板重新生成。是否仍要继续初始化？"

### 0.2 生成文件夹名称

命名规则：`类型标签-简称`

```
都市爽文 + 大医无双 → 都市-大医
无限流修仙 + 万界融合 → 无限流-万界
玄幻 + 吞天记 → 玄幻-吞天
悬疑 + 暗夜追踪 → 悬疑-暗夜
```

如果生成的名称与已有文件夹冲突，追加数字后缀：`都市-大医2`

向用户确认文件夹名称：
> "将为新小说创建文件夹 `d:\ai小说\都市-大医\`，确认？"

### 0.3 创建项目结构

确认后，一次性创建：

```
d:\ai小说\{文件夹名}\
├── novel-config.md         ← 从总大纲自动生成
├── plan\
│   ├── 总大纲.md           ← 保存用户提供的总大纲
│   └── 黄金三章_微操细纲.md ← 细化大纲时自动生成（如用户已提供则直接保存）
├── 正文\                   ← 空，等待写草稿
├── 审查报告\               ← 空，等待审查
├── 章节总结\               ← 空
└── 阶段总结\               ← 空
```

### 0.4 自动生成 novel-config.md

从总大纲中提取以下字段，自动填入 novel-config.md：

| novel-config 字段 | 从总大纲提取来源 |
|------------------|----------------|
| 书名 | 大纲标题 |
| 类型 | 大纲中的类型标注 |
| 目标字数/章 | 大纲中的字数规划，如无则留空让用户补 |
| 总章数规划 | 大纲中的总章数 |
| 阶段划分 | 大纲"阶段划分表"（如存在） |
| 写作铁律（含节奏规则） | 大纲"写作铁律"（如存在） |
| 角色档案 | 大纲"角色档案与发展轨迹"（如存在） |
| 伏笔追踪表 | 大纲"伏笔追踪表"（如存在） |
| 世界观设定 | 大纲"世界观设定"（如存在） |

如果总大纲中缺少某个字段（比如用户在外部平台生成时没按提示词模板的约束来），novel-config.md 中对应字段留空并标注 `【需补充】`，同时提示用户：
> "novel-config.md 中以下字段为空，建议补充：XXX。这些字段对后续草稿生成和审查至关重要。"

### 0.5 初始化完成提示

```
新小说项目已创建完成！

📁 d:\ai小说\都市-大医\
  ├── novel-config.md     ✅ 已从总大纲生成
  ├── plan\总大纲.md      ✅ 已保存
  ├── 正文\               📭 等待写草稿
  ├── 审查报告\           📭 等待审查
  ├── 章节总结\           📭 等待生成
  └── 阶段总结\           📭 等待生成

下一步建议：
1. 检查 novel-config.md 中标注【需补充】的字段
2. 使用"细化大纲"生成第一阶段或第1-50章归档段的逐章大纲
3. 使用"写第1章"开始创作
```

---

## 阶段1：大纲生成

**定位**：用户通常会用更好的模型在外部平台生成总大纲，本阶段主要提供提示词模板。

**执行步骤**：

1. 读取提示词模板文件 `prompts/大纲生成提示词.md`
2. 提示用户：请在其他平台使用此提示词生成总大纲。生成后需要：
   - 将总大纲保存到 `plan/` 目录
   - 根据总大纲更新 `novel-config.md` 中的阶段划分表、写作铁律、角色档案
3. 如果用户要求在本平台直接生成，则使用同一套约束规则，但提醒用户本平台模型可能不如外部大模型

---

## 阶段2：分阶段大纲细化

**定位**：在工作模型上批量生成分阶段大纲。

**执行步骤**：

1. 读取 `novel-config.md` 获取阶段划分和写作约束
2. 读取 `plan/` 下的总大纲
3. 读取 `阶段总结/` 下所有阶段总结（已完成阶段 + 当前进行中阶段）
4. 组装细纲上下文：读取全部阶段总结、当前细化范围之前100章细纲（例如生成第201-250章时读取第101-200章细纲，前文不足100章则读取全部已有细纲）、最近5章正文全文、最近10章章节总结（必须包含最近5章正文对应总结）。如果这是第一阶段且没有前文则跳过前文层。以细化起始章 S 计算：最近5章正文为第 `max(1, S-5)` 到第 `S-1` 章；最近10章章节总结为第 `max(1, S-10)` 到第 `S-1` 章。章节总结层必须包含最近5章正文对应的总结，不要因为正文已读就顺延或跳过，因为总结中包含硬事实、角色认知边界、信息流边界、因果债和后文禁止误写等正文未必显式提及的规则。
5. 执行细化窗口边界：按 `count = end - start + 1` 判断；`count <= 50` 可一次生成一个50章归档段，`count > 50` 则停止并要求按50章归档段拆分。分阶段大纲细化属于规划层任务，不适用正文创作的五章直写闸门
6. **黄金三章检查**：如果当前细化范围包含第1-3章，且 `plan/黄金三章_微操细纲.md` 不存在，则必须先生成黄金三章微操细纲（见阶段2.1），再继续生成常规逐章大纲。黄金三章细纲是对第1-3章的微操级补充，常规大纲中的第1-3章必须与黄金三章细纲保持一致
7. 使用 `prompts/分阶段大纲细化提示词.md` 中的约束规则，生成指定窗口的逐章大纲。如果存在黄金三章细纲，生成第1-3章大纲时必须以黄金三章细纲为准
8. 输出到 `plan/大纲_第N-N+49章.md`（N为50的整数倍+1）。例如细化第101-105章时写入 `大纲_第101-150章.md`，细化第106-110章时继续追加/更新同一文件。如果该文件已存在，只追加/覆盖对应章节的大纲，不要新建5章文件

### 2.1 黄金三章微操细纲生成

**定位**：前三章决定读者去留，需要比常规大纲更精细的微操级指引。

**触发条件**：细化范围包含第1-3章，且 `plan/黄金三章_微操细纲.md` 不存在

**执行步骤**：

1. 读取 `novel-config.md`（重点：角色档案+性格规格卡、写作铁律、伏笔追踪表、世界观设定）
2. 读取 `plan/` 下的总大纲（重点：阶段1描述、核心卖点）
3. 使用 `prompts/黄金三章微操细纲提示词.md` 中的约束规则，生成第1-3章的微操细纲
4. 输出到 `plan/黄金三章_微操细纲.md`

**与常规大纲的关系**：黄金三章细纲是第1-3章的"超集"——它包含常规大纲的所有字段，还额外包含情绪节拍、台词设计、避坑指南等微操级指令。生成常规大纲时，第1-3章以黄金三章细纲为准，不再另行细化

---

## 阶段3：草稿生成

**定位**：根据大纲写正文草稿。核心是**上下文组装**——确保 AI 有足够的"记忆"来写下一章。

### 3.1 上下文组装（自动执行，写每章前必做）

```
读取优先级（由远到近，由粗到细）：

第1层：全部阶段总结
  → 读取 阶段总结/ 下所有阶段总结，包括已完成阶段和当前进行中的阶段总结
  → 作用：防止死人复活、关系回退、能力越级、长期时间线错位等跨阶段硬伤；掌握本阶段内的事件走向、角色当前状态和关键时间锚
  → 预估token：每阶段约500-1500字，总量可控
  → 边界处理：当前阶段总结尚未生成时跳过该文件，但仍读取已有阶段总结

第2层：近3章正文全文
  → 读取当前章节之前3章的全部正文内容（如写第31章时读取第28-30章全文）
  → 作用：确保场景、对话、情绪、细节的精确连续性；掌握近3章内所有角色的完整状态变化
  → 预估token：每章约2000-3000字，3章共约6000-9000字
  → 边界处理：前面不足3章时，读取全部已有章节正文（如写第3章时读取第1-2章全文）
  → ⚠️ 这是保证文风一致性和剧情连贯性的核心层——必须读取全文，不能只读总结

第3层：近20章章节总结（包含近3章正文对应总结）
  → 读取第max(1, N-20)章到第N-1章的章节总结；第N-3到第N-1章即使已经读取正文全文，也必须继续读取对应章节总结
  → 章节总结层不顺延、不排除正文层。原因：正文负责场景、语气和细节连续；章节总结负责硬事实、角色认知边界、信息流边界、因果债和后文禁止误写等执行规则，正文中未必显式提及这些规则
  → 跨文件读取：当章节范围跨越50章分块边界时，必须从多个章节总结文件中拼接，不得只读当前50章分块。例：写第61章时，应读取 `章节总结_第1-50章.md` 的第41-50章部分 + `章节总结_第51-100章.md` 的第51-60章部分；忽略前一文件会导致中期剧情、伏笔和角色关系上下文断裂
  → 作用：掌握中期剧情脉络、伏笔回收、角色关系变化和必须遵守的逻辑规则，同时控制上下文规模
  → 预估token：每章总结约300-800字，最多约20章，总量可控
  → 注意：更久远的剧情、伏笔和角色状态通过全部阶段总结兜底，不读取第1章到第N-21章的全量章节总结，除非被伏笔原章补读命中
  → 边界处理：前文没有章节总结时，跳过此层

第4层：本章完整大纲 + 后续2章边界大纲
  → 读取分阶段大纲中本章的完整详细描述
  → 同时读取第N+1、N+2章的轻量边界：章节名、核心事件、关卡/阶段安排、章末钩子、不得提前写入本章的内容。只提取边界信息，不粘贴后续章节完整详细大纲，避免上下文膨胀
  → 若后续2章跨越50章分块，按50章分块定位规则读取对应大纲文件中的目标章节块；若后续章节大纲不存在，写明"后续边界大纲缺失"，不要臆造
  → 作用：明确本章必须发生的事件和爽点，同时明确后续章节的内容边界，防止多章内容被单章写完、后续章节提前消耗
  → 如果本章是第1-3章且 `plan/黄金三章_微操细纲.md` 存在，优先以黄金三章细纲为准；仍需读取第N+1、N+2章边界，避免提前消耗后续章节

条件补读：伏笔埋设原章与对应章节总结
  → 在读取本章完整大纲和后续2章边界后，检查本章大纲/后续边界中的核心事件、伏笔操作、章末钩子是否对应阶段总结或 novel-config.md 伏笔追踪表中的未回收/待回收伏笔
  → 若命中某条伏笔，读取该伏笔的埋设章节正文并加入上下文；如果埋设章节已经在近3章正文全文中，则不重复读取正文
  → 同时读取该伏笔埋设章节对应的章节总结并加入上下文；如果该总结已经包含在近20章章节总结中，则不重复读取总结。原因：正文保留原始描写，章节总结保留硬事实、角色认知边界、信息流边界、因果债和后文禁止误写，二者缺一不可
  → 若埋设章节总结不存在，必须标注"伏笔埋设章总结缺失"，不得凭正文临时编造总结规则；可继续使用原章正文，但在逻辑依赖表中将缺失项列为待确认
  → 匹配依据包括：伏笔名称、关键词、道具/人物/地点/能力名、"第X章埋/计划第N章回收/安全期限"等描述。不能只凭模糊联想强行匹配
  → 作用：回收或推进伏笔时保留原始埋设细节，避免把伏笔写错、写漏、重复解释或改变原意
  → 控制规模：每章最多补读3个埋设章节；若命中超过3条，优先读取与本章大纲明确标注"收/回收/揭示/兑现"的伏笔，其余列为"待确认"，不要自行全量补读

派生层：本章逻辑依赖表（写前整理，置于生成提示词前部）
  → 从全部阶段总结、近3章正文全文、近20章章节总结、伏笔埋设原章、本章大纲和后续2章边界中提取本章必须遵守的逻辑约束
  → 必须包含：硬事实、角色认知边界、能力/资源状态、关系/情绪状态、信息流边界、因果债/未解决冲突、后文禁止误写
  → 作用：把长上下文中的关键证据钉成短约束，防止 AI 读过但在生成时忽略；尤其防止"前文已证明/已解释的设定，后文又当成不存在、不合理、第一次出现来写"
  → 规则：逻辑依赖表只能来自已读取上下文和大纲，不得凭空补设定；无法确定时写"待确认"，不得把猜测写成硬事实

第5层：novel-config.md
  → 读取写作铁律、角色档案、伏笔追踪表
  → 作用：约束生成过程中的行为边界
```

如果某个层级的文件不存在（如阶段总结还未生成），跳过该层级并提示用户。

### 3.1.1 写前大纲-上下文校准闸门（强制阻断）

每写一章前，必须先执行本闸门；这是硬性流程，不是写作建议。只有校准通过后，才能调用 `prompts/草稿生成提示词.md` 生成正文。

1. 从本章完整大纲提取：章节号、章节名、起始状态、必须发生的核心事件、出场角色、爽点/伏笔操作、章末钩子。
2. 从后续2章边界大纲提取：后续章节名、后续核心事件、后续关卡/阶段安排、不得提前写入本章的事件清单。
3. 从阶段总结和 novel-config.md 伏笔追踪表中匹配本章大纲涉及的伏笔；如命中，确认埋设章节是否已纳入近3章正文，未纳入则补读埋设原章；同时确认埋设章对应章节总结是否已纳入近20章总结，未纳入则补读对应章节总结。
4. 从近3章正文全文、近20章章节总结和伏笔埋设原章提取：已经完成的事件、上一章章末状态、伏笔原始埋法、只被预告但尚未发生的事件。近20章总结必须包含近3章正文对应总结，用于提取正文未必明说的硬事实、角色认知边界、信息流边界、因果债和后文禁止误写。
5. 生成本章逻辑依赖表：硬事实、角色认知边界、能力/资源状态、关系/情绪状态、信息流边界、因果债/未解决冲突、后文禁止误写。硬事实和角色认知必须分开，例如"古籍有载"是客观事实，"某角色亲眼见过/相信/否认"是角色认知。
6. 对齐判定：
   - 如果本章大纲的核心事件已经在前文正文或总结中作为“已完成事件”出现，判定为大纲-正文错位，立即停止，不得继续写本章。
   - 如果上一章章末状态已经推进到本章之后的阶段（例如上一章已宣布第三关开始，而本章大纲仍要求写第一关），判定为大纲-正文错位，立即停止。
   - 如果本章构思或正文将写入后续2章边界大纲中的核心事件，且本章大纲没有明确要求，判定为后续章节提前消耗，必须收束回本章大纲边界，不得继续扩写。
   - 如果本章构思会把前文已确立的硬事实反向解释为"不存在/不合理/第一次出现"，判定为前文证据冲突，必须改为符合证据的写法或停止等待人工确认。
   - 如果某角色说出/利用其不应知道的信息，或把个人未见过误写成世界客观不存在，判定为角色认知越界或信息流越界，必须收束到角色认知边界。
   - 如果人物行动缺少因果桥、动机桥或与上一章情绪/关系状态断裂，判定为因果断裂/状态回退，必须补桥或停止。
   - 如果前文只是预告、约定、宣战或提出下一关，但事件尚未执行完成，不算错位，可以继续写。
   - 如果本章大纲与前文上下文冲突，且无法同时满足二者，停止并向用户报告冲突，不得擅自改写大纲、跳章、合并章节或顺着前文写后续剧情。
7. 阻断时输出：

```markdown
大纲-正文错位，暂停写作
- 当前章节：第X章《章节名》
- 本章大纲应写：...
- 前文已经写到：第Y章 ...（引用关键句或总结）
- 风险：继续写会偏离大纲/提前消耗后续章节
- 建议处理：
  1. 回修第Y章，把提前写出的内容撤回到对应章节
  2. 人工确认重排大纲后再继续
```

8. 直写窗口内任一章节触发阻断时，停止当前章节及后续章节生成，先等待用户确认修复方案。

### 3.2 五章内直写与长任务脚本分流

大量章节不应在单次会话中连续生成，否则注意力会漂移、剧情容易写偏。正文创作改为**当前会话最多直写5章**；超过5章时，不在 skill 内硬写，改由批量写作脚本统一编排。

**执行单位 vs 归档单位**：
- **当前会话直写单位**：1-5章。例如 `第101-105章` 可直接写。
- **长任务单位**：用户要求写6章及以上时，启动批量写作脚本（计划路径：`d:\ai小说\提示词和脚本\novel_batch_writer.py`，待实现），由脚本决定如何分批、压缩上下文、继续任务。
- **归档单位**：50章。章节总结、审查报告、分阶段大纲仍按 `第101-150章` 这类文件保存。
- 示例：写完第101-105章后，把101-105章总结写入 `章节总结_第101-150章.md`；后续写第106-110章时，继续追加/更新同一个文件里的106-110章内容。

**范围处理**：
1. 用户指定 `count <= 5` 个章节：直接执行该范围。
2. 用户指定 `count > 5` 个章节：不直接写作，启动批量写作脚本。若脚本不存在或不可执行，停止并提示用户先实现/配置脚本。
3. 用户说"继续写/接着写"且未指定范围：从当前最高已完成章节的下一章开始，默认直写下一组5章。

**⚠️ 禁止子代理写作（强制约束）**：

章节写作必须在主进程中顺序执行，**禁止**使用 Agent/子代理并行写章节。原因：
- 子代理无法继承主进程累积的上下文（全部阶段总结、近3章正文全文、近20章章节总结等）
- 并行写多批章节会导致批次间衔接断裂、人物状态不一致、伏笔丢失
- 每一章都依赖前一章的精确状态，必须串行

此约束覆盖：草稿生成、章节总结、审查修复等所有涉及正文内容的操作。仅纯信息查询（如搜索文件、读取大纲）可使用子代理。

**五章内直写流程**：
```
1. 解析本次章节范围并通过任务范围闸门
2. 每章写前组装上下文（按3.1五层结构：全部阶段总结→近3章全文→近20章章节总结（含近3章对应总结）→本章完整大纲+后续2章边界大纲→novel-config），并派生本章逻辑依赖表
3. 执行写前大纲-上下文校准闸门；若发现前文已提前写完本章大纲事件，立即停止并报告，不生成正文
4. 校准通过后，按章节顺序写本轮草稿，每写完一章立即生成/更新章节总结
5. 本轮全部完成后，更新当前阶段总结
6. 输出本轮完成摘要，然后停止；不自动继续下一组
```

**本轮完成摘要格式**：

```
本轮完成：第X-Y章
已写入正文：X份
已更新章节总结：X份（写入/更新到 `章节总结_第N-N+49章.md`）
阶段总结：已更新至第Y章
下一轮建议：继续写第Y+1-Y+5章，或在需要写6章及以上时启动批量写作脚本
```

不要在完成摘要后继续写下一章；下一组必须由用户再次触发、由新的"继续写"请求触发，或由批量写作脚本接管。

**跨阶段边界的处理**：
- 写新阶段第1章时，第2层读取上一阶段最后3章的全文
- 第3层按当前章节号读取近20章章节总结（包含已读取全文的近3章对应总结），跨阶段时从对应50章总结分块中取需要的章节
- 第1层读取全部阶段总结；阶段2总结此时可能为空或仅有大纲预填，随写作推进逐步填充
- 这种设计确保跨阶段衔接有足够的细粒度信息，不需要额外增加衔接层

### 3.3 生成草稿（单章）

1. 执行3.1.1写前大纲-上下文校准闸门；未通过时停止，不得生成正文
2. 校准通过后，按 `prompts/草稿生成提示词.md` 中的写作约束执行
3. 正文输出到 `正文/第XXX章_章节名（草稿）.md`
4. 生成完毕后自动触发章节总结生成（阶段5）——确保写下一章时总结一定存在
5. 提示用户可进入草稿审查（阶段4）。审查通过后，用审查报告中的章节总结覆盖更新（如审查未修改正文，则总结不变）

---

## 阶段4：草稿审查

**定位**：最后一道关。只抓必须改的问题，改几句话就能提升的也报。

### 4.1 审查执行

1. 读取待审查章节的正文
2. 读取待审查章节的本章完整大纲 + 后续2章边界大纲、novel-config.md（必须包含角色性格规格卡）。若审查多个章节，对每章分别读取其对应的后续2章边界，用于判断是否提前消耗后续章节
3. 读取全部阶段总结 + 审查范围前3章正文全文 + 审查范围前20章章节总结（包含已读取全文的近3章对应总结；用于检查场景衔接、时间线矛盾、重复设定、角色认知边界、信息流边界、后文禁止误写等需要细粒度上下文的问题，同时避免上下文爆炸）。读取阶段总结时必须提取其中的"关键时间锚"，用于审查长期时间线、限时事件、伏笔安全期和阶段衔接
4. 若待审查章节正文或对应大纲涉及阶段总结/伏笔追踪表中的未回收或待回收伏笔，读取对应埋设章节正文和对应章节总结作为伏笔补读；埋设章已在前3章正文中时不重复读取正文，埋设章总结已在前20章总结中时不重复读取总结；每章最多补读3个埋设章节
5. 执行任务范围闸门：按 `count = end - start + 1` 判断；`count <= 5` 直接审查，`count > 5` 则停止并要求用户拆成不超过5章的窗口，或等待后续专用批处理脚本
6. 量化统计脚本调用约束：字数统计、AI味/反应词密度扫描等审查辅助脚本只能从 `d:\ai小说\提示词和脚本` 调用；不得调用工作区根目录散落脚本或单本小说项目目录中的脚本。EPUB/MOBI/封面/打包脚本属于项目专属脚本，不参与草稿审查统计。
7. 审查前先为每章派生"审查逻辑依赖表"：硬事实、关键时间锚、角色认知边界、能力/资源状态、关系/情绪状态、信息流边界、因果债/未解决冲突、后文禁止误写。所有判断必须引用正文、总结或大纲中的证据；不确定项列为风险，不得伪造证据。关键时间锚只来自阶段总结"关键时间锚"、章节总结中的硬时间标注或正文明确时间句，不得把日常时间流水扩写成硬约束
8. 按 `prompts/草稿审查提示词.md` 中的审查清单逐项检查，分为四类：
   - **A类硬伤**：大纲偏离（含章节节奏压缩、后续章节提前消耗、多章内容被单章写完）、前文证据冲突、角色认知越界、信息流越界、因果断裂、状态回退、伏笔含义变形、死人复活、能力越级、关系回退、时间线矛盾（必须对照关键时间锚）、场景跳跃、重复设定、性格违规（禁写清单）——零容忍
   - **B1人物性格偏移**：对话辨音、行为模式偏移、情绪表达偏移——对照性格规格卡检查
   - **B2节奏问题**：打脸完整性（4拍）、场景内拖沓（800字无新事件）、字数不达标（低于目标下限70%，统计口径：中文字符+中文标点逐字计数，省略号"……"算6字符、破折号"——"算2字符，空格换行不计）、连续压抑、章末无钩子、升级无验证、大场面后无喘息
   - **B3 AI味**：反应词密度（量化扫描）、告诉代替展示、旁白过度介入、重复解释、审问式对话、水对话——看数据，按草稿生成阈值统一报警：第一梯队任一词≥2次、第二梯队累计>3次、第三梯队累计>5次即报
9. 输出审查报告，包含：
   - 审查进度表（见4.2格式）
   - 总体评价与质量评级
   - **修复情况**（占位，审查阶段留空；只有用户明确要求"审查并修复"或"修复第X章"后，才由阶段4.4填充）
   - A/B类问题详情（每条附修改建议）
   - 章节总结（格式与阶段5一致，审查通过后覆盖更新到 `章节总结/`）
   - 阶段总结更新建议（如适用）
10. 将审查报告保存到 `审查报告/审查_第N-N+49章.md`（N为50的整数倍+1，如1-50、51-100、101-150）。如果本次只审查第101-105章，也写入 `审查_第101-150章.md`，文件中暂时只包含101-105章结果；之后审查106-110章时，继续追加/更新同一文件。若重审同一章或同一范围，只替换对应章节内容，不重复追加
11. 更新章节文件状态：
   - 无问题的章节：`第XXX章_章节名（草稿）.md` → `第XXX章_章节名.md`（去掉状态标签，成为正式稿）
   - 有问题的章节：`第XXX章_章节名（草稿）.md` → `第XXX章_章节名（已审查）.md`（标记为待修复）
12. **审查完成后默认停止，不自动修复**。向用户说明：本次只完成审查；只有用户明确说"审查并修复"、"审查后修复"或"修复第X章"时，才进入阶段4.4。

### 4.2 审查进度表格式

每份审查报告开头必须包含进度表，方便后续追踪修复状态：

```markdown
## 审查进度

| 章节 | 状态 | A类 | B类 | 修复状态 |
|------|------|-----|-----|---------|
| 第1章 | ✅ 通过 | 0 | 1 | — |
| 第2章 | ⚠️ 有问题 | 1 | 2 | 未修复 |
| 第3章 | ✅ 通过 | 0 | 0 | — |

- 已修复：0/1章（有问题的章节中）
- 整体状态：审查中
```

状态说明：
- **状态列**：✅ 通过（无A类）/ ⚠️ 有问题（有A类）
- **修复状态列**：未修复 / 已修复 / —（无需修复）
- **整体状态**：审查中（有章节未修复）/ 已完成（全部修复或通过）

### 4.3 修复确认（用户手动修复后使用）

当用户说”修好了”/”已修复”/”第X-X章修完了”时（用户自行修改了正文，需要确认修复状态）：

1. 确认用户修复了哪些章节（从用户消息中解析，如”第1-30章修好了”；如不明确则询问）
2. 读取修复后的正文，确认A类问题是否已解决
3. 对修复部分做局部复查（参照4.4的局部复查规则）
4. 更新文件名：`第XXX章_章节名（已审查）.md` → `第XXX章_章节名.md`
5. 更新审查报告中的进度表和修复情况块
6. 如所有章节均已修复，更新阶段总结（阶段6）
7. 用修复后的正文重新生成章节总结，覆盖更新到 `章节总结/`

### 4.4 自动修复（显式触发）

**定位**：仅在用户明确要求"审查并修复"、"审查后修复"、"修复第X章"/"fix"时执行，根据修复判定规则修改正文，然后局部复查。

**触发时机**：
- 用户说"审查并修复第X章"/"审查后修复"
- 用户说"修复第X章"/"fix"
- 纯"审查"请求不得触发本阶段；审查完成后只提示用户可继续要求"审查并修复"或"修复第X章"

**执行步骤**：

1. 读取审查报告，提取所有A/B类问题；如果没有可用审查报告，先要求用户执行审查
2. 执行任务范围闸门：按 `count = end - start + 1` 判断；`count <= 5` 直接修复，`count > 5` 则停止并要求用户拆成不超过5章的窗口，或等待后续专用批处理脚本
3. 解析待修复章节正文文件；若同一章存在多个正文文件，停止并列出冲突文件，请用户确认后再继续
4. 读取待修复章节的正文、对应大纲、写作铁律、角色档案
5. 按 `prompts/草稿自动修复提示词.md` 中的修复判定规则，先生成修复方案和修复后正文，对每个问题分类：
   - **必须修复**：A类全部、B3超标2倍及以上、B1/B2在核心场景的偏移
   - **AI判断并尽量修复**：B1日常场景偏移、B2节奏问题、B3 AI味超标但未达2倍、审查报告中明确标出的局部AI味问题（逐项判断，见提示词）
   - **A3/AI味标注兼容**：如果审查报告或用户表述把“AI味”写成“A3类/A3问题”，按 B3 AI味处理，由AI判断并在不改变剧情、人设、信息量的前提下轻修
   - **不修复**：B3未超标且读感自然、审查标注OK、需增加大段新内容
6. 对”必须修复”和”判断为修复”的问题，写入修复后正文到对应章节文件
7. **局部复查**：只对修改涉及的段落重跑相关检查项（不全章重审）。复查项按修改类型匹配：
   - A类修复 → 复查大纲偏离/前文证据冲突/角色认知越界/信息流越界/因果断裂/状态回退/伏笔含义变形/死人复活/能力越级/关系回退
   - B1修复 → 复查对话辨音/行为模式/情绪表达
   - B2打脸修复 → 复查打脸完整性
   - B2拖沓修复 → 复查场景内拖沓
   - B3修复 → 复查反应词密度/万能副词密度
   - B3告诉→展示 → 复查性格规格卡一致性
8. 复查发现新引入的A类硬伤 → 立即再修；新引入的B类轻微问题 → 放过或记入未修复原因
9. 更新审查报告：
   - 在”修复情况”块中填充已修复/未修复/修复前后对比/局部复查结果
   - 更新审查进度表的修复状态
   - 更新整体状态
10. 更新章节文件名：
   - A类硬伤已全部消除的章节 → 去掉状态标签（正式稿）
   - 仍有A类硬伤的章节 → 保留`（已审查）`标签
11. 用修复后的正文重新生成章节总结，覆盖更新到 `章节总结/`
12. 复读正文文件、审查报告、章节总结和文件名状态，验证落盘成功且目标文件存在；任何一步失败都停止并报告失败点，不得声称完成
13. 如所有章节A类硬伤已消除，更新阶段总结（阶段6）
14. 向用户输出修复情况摘要：”已修复X项（A类Y项/B类Z项），未修复X项（原因见审查报告），局部复查无新问题，已验证落盘”

---

## 阶段5：章节总结生成

**定位**：为每章生成结构化总结，供后续续写时作为上下文。

**触发时机**：
- 草稿生成后自动触发（确保写下一章前总结一定存在）
- 草稿审查通过后，用审查报告中的章节总结覆盖更新（如审查未修改正文，则总结不变）
- 用户手动要求"总结第X章"时
- 会话结束流程中，补齐未生成的章节总结

**执行步骤**：

1. 读取本章正文
2. 读取本章大纲（用于对照）
3. 输出章节总结。时间信息不新增固定字段；只有当本章时间本身构成硬事实、行动合理性、倒计时、伏笔安全期或下一章衔接条件时，才写入"本章确立的硬事实""因果债与未解决冲突"或"章末状态"。不得把每章日常时间流水写入章节总结。
4. 输出格式：

```markdown
<!-- chapter:X summary:start -->
# 第X章 总结

## 关键事件
- （1-3条）

## 角色状态变化
- 角色A：状态变化描述

## 本章确立的硬事实
- 事实：证据/原文依据（没有则写"无新增"）

## 角色认知变化
- 角色A：知道/相信/误解/否认了什么（区分"客观事实"与"角色认知"）

## 能力/资源变化
- 主角/关键角色：能力、等级、道具、资源的获得/消耗/限制

## 信息流边界
- 谁知道真相：
- 谁只知道传闻/误解：
- 谁不应拥有作者视角：

## 因果债与未解决冲突
- 因果债：本章造成但尚未兑现的后果
- 未解决冲突：仍在持续的矛盾

## 伏笔操作
- 埋/收/悬：描述

## 后文禁止误写
- 不得把已确立事实写反：
- 不得让角色认知越界：
- 不得提前消耗后续事件：

## 章末状态
- 主角位置：
- 主角能力/等级：
- 当前冲突：
- 下章预告（从大纲推断）：
<!-- chapter:X summary:end -->
```

5. 保存到 `章节总结/章节总结_第N-N+49章.md`（N为50的整数倍+1）。如果本次写完第101-105章，保存到 `章节总结_第101-150章.md`；写完第106-110章时，继续追加/更新同一文件。若该文件已存在，按 `summary:start/end` 锚点替换该章对应内容，不要重复追加，不要新建5章文件

---

## 阶段6：阶段总结更新

**触发时机**：
- 用户说"更新总结"
- 一个阶段的所有章节写完时自动触发
- 草稿审查完成一个阶段时自动触发
- 会话结束流程中自动执行

**执行步骤**：

1. 读取本阶段所有章节总结
2. 读取已有的阶段总结（如果存在）
3. 合并更新时必须提取"关键时间锚"：只记录会影响后文逻辑、角色关系、伏笔回收、行动倒计时、阶段衔接的重要时间；不得记录每章日常时间流水。优先记录开篇/阶段起点时间、同日/次日/数日后等关键连续性、限时行动、检测/收网/约定期限、伏笔埋设与回收时间。
4. 输出格式：

```markdown
# 阶段总结：阶段名（第X-X章）

## 关键事件
- 按时间顺序列出本阶段所有重要事件

## 关键时间锚
- 时间锚：影响后文逻辑/伏笔/倒计时的关键时间事实（依据章节）。若本阶段无重要时间锚，写"无新增关键时间锚"。

## 角色状态快照
- 角色A：当前等级/能力/资源/位置（标注截至第X章）
- 角色B：当前状态（标注截至第X章）

## 角色死亡/退出
- 角色C：第X章，死亡/退出原因（如无则写"无"）

## 关系变化
- 角色A↔角色B：起始关系 → 结束关系

## 主角能力/资源
- 等级：X（第X章升级）
- 技能：XXX
- 资源：XXX

## 关键硬事实
- 已确立事实：证据章节/依据

## 信息流边界
- 知情者：
- 误解者：
- 未知情者：

## 因果债与未解决冲突
- 因果债：
- 未解决冲突：

## 后文禁止误写
- 禁止误写项：依据章节/原因

## 未回收伏笔
- 伏笔描述（第X章埋，计划第X章回收，安全期限X章）
```

5. 保存规则（优先级从高到低）：
   - **如果 novel-config.md 有阶段划分表**：按阶段保存，文件名为 `阶段N_阶段名_第X-Y章.md`（X-Y 为阶段划分表中该阶段的章节范围，如 `阶段2_裂隙吞噬_51-120章.md`）
   - **如果 novel-config.md 没有阶段划分表**：按50章一块保存，规则与章节总结一致。给定章节号 X，所在分块为 `N = ((X-1) // 50) * 50 + 1`，文件名为 `阶段总结_第N-N+49章.md`（如 `阶段总结_第1-50章.md`、`阶段总结_第51-100章.md`）
   - 如果该文件已存在，追加/覆盖对应内容，不要新建文件
6. 如果本阶段全部写完，在对应阶段总结段落标记 `[状态：已完成]`，否则标记 `[状态：进行中·截至第X章]`

---

## 阶段7：一致性检查

**定位**：跨章节/跨阶段的硬伤扫描。

**执行步骤**：

1. 读取所有阶段总结 + 近3章全文 + 近20章章节总结（包含近3章对应总结）
2. 读取 novel-config.md 中的角色档案和伏笔追踪表
3. 逐项检查：

| 检查项 | 方法 |
|--------|------|
| 死人复活 | 阶段总结中"角色死亡/退出"名单 vs 近期正文中是否出现同名角色在场 |
| 能力越级 | 阶段总结中"主角能力/资源"快照 vs 正文中是否使用了未解锁能力 |
| 关系回退 | 阶段总结中"关系变化"链条 vs 近期正文是否出现关系倒退 |
| 伏笔烂尾 | novel-config.md 中伏笔追踪表 vs 当前章节号，检查是否超过安全期限 |
| 时间线矛盾 | 阶段总结"关键时间锚" + 相邻章节总结中的硬时间标注 vs 是否出现时间倒流、长期时间锚错位或限时事件过期 |
| 重复设定 | 近10章总结 vs 是否有同一设定被重新解释且说法不同 |
| 前文证据冲突 | 章节总结"本章确立的硬事实/后文禁止误写" vs 近期正文是否把已确立事实写反 |
| 角色认知越界 | 章节总结"角色认知变化/信息流边界" vs 近期正文是否让角色知道不该知道的信息，或把个人未知误写成客观不存在 |
| 信息流泄露 | 知情者/误解者/未知情者列表 vs 对话和行动中是否出现作者视角泄露 |
| 因果断裂 | 章节总结"因果债与未解决冲突" vs 后续正文是否跳过动机桥、后果桥或冲突承接 |
| 伏笔含义变形 | 伏笔埋设原章/总结中的原始含义 vs 回收章节是否改写成另一套含义 |

4. 输出一致性报告：

```markdown
# 一致性检查报告

## 硬伤（必须修）
- **[类型]** 第X章 vs 第X章：矛盾描述 → 建议修改方案

## 风险（需要确认）
- **[类型]** 第X章：疑似问题 → 需要确认

## 伏笔状态
- 超期未回收：XXX（第X章埋，安全期限第X章，当前第X章）
- 即将到期：XXX（第X章埋，安全期限第X章，当前第X章）

## 通过项
- 死人复活：无
- 能力越级：无
- 关系回退：无
- 时间线：无矛盾
- 前文证据冲突：无
- 角色认知/信息流：无越界
- 因果承接：无断裂
```

---

## 文件夹结构约定

每个小说项目的标准结构：

```
小说名/
├── novel-config.md         # 必须存在，包含所有配置
├── plan/                   # 大纲文件
│   ├── 总大纲.md
│   ├── 黄金三章_微操细纲.md
│   ├── 大纲_第1-50章.md
│   └── 大纲_第51-100章.md
├── 正文/                   # 章节正文，每章一个文件
│   ├── 第001章_绝境（草稿）.md
│   ├── 第002章_交锋（已审查）.md
│   ├── 第003章_突破.md
│   └── ...
├── 审查报告/               # 每50章一个文件
│   ├── 审查_第1-50章.md
│   └── 审查_第51-100章.md
├── 章节总结/               # 每50章一个文件
│   ├── 章节总结_第1-50章.md
│   └── 章节总结_第51-100章.md
└── 阶段总结/               # 按大纲阶段划分，一个阶段一个文件
    ├── 阶段1_重生蛰伏_1-50章.md
    └── 阶段2_裂隙吞噬_51-120章.md
```

**分块规则**：
- 章节总结、审查报告、分阶段大纲：按50章一个文件保存。文件名中 N 为50的整数倍+1（1、51、101…）。5章只是当前会话直写/直审上限，不改变50章归档。保存时先检查目标文件是否已存在——已存在则按锚点替换对应章节内容，不存在则新建
- 阶段总结：按 novel-config.md 阶段划分表中的阶段保存，一个阶段一个文件。文件名为 `阶段N_阶段名_第X-Y章.md`（如阶段2范围是51-120章，则文件名为 `阶段2_裂隙吞噬_51-120章.md`）。保存时先检查目标文件是否已存在——已存在则追加/覆盖，不存在则新建
- 正文文件：每章一个文件存放

**归档锚点规则（强制）**：
- 分阶段大纲、章节总结、审查报告、修复情况中，每个章节块必须使用稳定HTML注释锚点：
  - 大纲：`<!-- chapter:101 outline:start -->` 到 `<!-- chapter:101 outline:end -->`
  - 总结：`<!-- chapter:101 summary:start -->` 到 `<!-- chapter:101 summary:end -->`
  - 审查：`<!-- chapter:101 review:start -->` 到 `<!-- chapter:101 review:end -->`
  - 修复：`<!-- chapter:101 repair:start -->` 到 `<!-- chapter:101 repair:end -->`
- 重生成、重审或重修某章时，只替换对应锚点之间的内容，不得重复追加，也不得覆盖同一50章文件中的无关章节
- 若目标章节没有锚点，先追加带锚点的新章节块；若锚点不成对或重复，停止并请用户确认

---

## 章节文件命名约定

正文文件的文件名包含章节状态标签，用于一眼看出哪些章节已审查、哪些待修复：

| 状态 | 文件名格式 | 含义 |
|------|-----------|------|
| 刚写完 | `第001章_绝境（草稿）.md` | 草稿生成后的初始状态，尚未审查 |
| 审查发现有问题 | `第001章_绝境（已审查）.md` | 审查完成但存在待修复问题 |
| 正式稿 | `第001章_绝境.md` | 审查通过（无问题）或问题已修复 |

状态流转：
```
（草稿）→ 审查通过无问题 → 去掉标签（正式稿）
（草稿）→ 审查发现问题 → （已审查）→ 用户修复 → 去掉标签（正式稿）
```

读取正文时按章节号匹配 `第XXX章_*.md`。同一章只能存在一个正文文件；若发现同一章同时存在草稿、已审查、正式稿或其他副本，必须停止并列出冲突文件，请用户确认，不得自动猜测。重命名前必须检查目标文件名是否已存在；若存在则停止，不得覆盖。

---

## 50章分块文件定位

章节总结、审查报告、分阶段大纲按50章一个文件保存。定位规则：

1. **计算文件名**：给定章节号 X，所在分块为 `N = ((X-1) // 50) * 50 + 1`，文件名为 `类型_第N-N+49章.md`
   - 第1章 → N=1 → `类型_第1-50章.md`
   - 第50章 → N=1 → `类型_第1-50章.md`
   - 第51章 → N=51 → `类型_第51-100章.md`
   - 第101章 → N=101 → `类型_第101-150章.md`
2. **跨分块读取**：如需读取第41-60章的章节总结，需读取 `章节总结_第1-50章.md` 的后半部分 + `章节总结_第51-100章.md` 的前半部分
3. **追加逻辑**：当前会话直写/直审通常不超过5章，但保存仍进入对应50章文件。第101-105章先写入 `章节总结_第101-150章.md` / `审查_第101-150章.md`；第106-110章完成后继续追加到同一文件。重生成或重审某章时，替换该章内容和进度表行，不重复追加

## 阶段总结文件定位

阶段总结的保存方式取决于 novel-config.md 是否有阶段划分表。

**有阶段划分表时**：按阶段保存，一个阶段一个文件。定位规则：
1. **计算文件名**：给定章节号 X，查找 novel-config.md 阶段划分表中 X 所属的阶段，文件名为 `阶段N_阶段名_第X-Y章.md`（X-Y 为该阶段的章节范围）
   - 第30章 → 属于阶段1(1-50) → `阶段1_重生蛰伏_1-50章.md`
   - 第80章 → 属于阶段2(51-120) → `阶段2_裂隙吞噬_51-120章.md`
2. **跨阶段读取**：如需读取多个阶段的总结，逐个读取对应文件
3. **追加逻辑**：保存时先检查目标文件是否已存在——已存在则追加/覆盖，不存在则新建

**没有阶段划分表时**：按50章一块保存，规则与章节总结一致。定位规则：
1. **计算文件名**：给定章节号 X，所在分块为 `N = ((X-1) // 50) * 50 + 1`，文件名为 `阶段总结_第N-N+49章.md`
   - 第1章 → N=1 → `阶段总结_第1-50章.md`
   - 第50章 → N=1 → `阶段总结_第1-50章.md`
   - 第51章 → N=51 → `阶段总结_第51-100章.md`
2. **跨分块读取**：如需读取多块的阶段总结，逐个读取对应文件
3. **追加逻辑**：保存时先检查目标文件是否已存在——已存在则追加/覆盖，不存在则新建

---

## novel-config.md 必须包含的字段

```markdown
# 小说配置

## 基本信息
- 书名：
- 类型：
- 目标字数/章：
- 总章数规划：

## 阶段划分
| 阶段 | 章节范围 | 名称 | 核心目标 |
|------|---------|------|---------|

## 写作铁律（含节奏规则）
1. （如：打脸不过夜——任何羞辱3章内回击）
2. （如：压抑:爽感=1:3）
3. （如：禁止连续3章纯压抑无爽点）

## 角色档案
### 角色名
- 身份：
- 初始状态：
- 发展轨迹：
- 退出方式：

#### 性格规格卡
- 性格核心：（1句话）
- 说话方式：语速/语气、口头禅、文化/职业特征、情绪表达方式
- 行为模式：面对冲突、面对弱者、面对强权、压力下异常行为
- 禁写清单：（绝对不能出现的行为/台词）

## 伏笔追踪表
| 伏笔 | 埋设章节 | 计划回收章节 | 安全期限 | 状态 |
|------|---------|------------|---------|------|

## 世界观设定
- 核心设定：
- 力量体系：
- 组织架构：
- 地图规划：
```

---

## 会话结束流程

当用户表示"今天就到这"/"写完了"/"结束"时，自动执行：

1. 检查本次会话写了哪些章节
2. 为所有未生成总结的章节生成章节总结
3. 更新当前阶段总结（合并新章节内容到阶段总结中）
4. 提示用户："已生成第X-X章的章节总结，阶段总结已更新至第X章"
