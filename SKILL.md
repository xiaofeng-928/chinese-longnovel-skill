---
name: my-novel
description: 长篇网络小说写作工作流。用于创建、续写、审查、修复、总结、扫榜或整理长篇小说项目，以及处理正文、章节、大纲、角色和伏笔。
---

# MyNovel 长篇入口

本公开仓库只包含长篇小说工作流。执行小说任务前，完整读取 `long-form/SKILL.md`，并只遵守其中的规则。

在 `D:\ai小说` 搜索文件或文本时，只使用 PowerShell 原生命令，例如 `Get-ChildItem`、`Select-String` 和 `Where-Object`；不使用 `rg`、`ripgrep` 或 `grep`。
