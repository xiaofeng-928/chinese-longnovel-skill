---
name: my-novel
description: 使用于在 D:\ai小说 中创建、续写、去 AI 润稿、审查、修复或整理小说项目，或处理小说正文、章节、大纲、角色、伏笔与扫榜等小说写作任务。
---

# MyNovel 小说写作入口

先判断任务是否属于小说写作。skill、代码、文档、配置或脚本工程等非小说对象，不进入本 Skill。

## 工作区与分流

- 长篇项目根目录：`D:\ai小说\小说`
- 短篇项目根目录：`D:\ai小说\短篇`
- MyNovel 根目录：`C:\Users\Lenovo\.claude\skills\MyNovel`

按以下规则选择分支：

1. 创建新项目时，以用户明确说明的“长篇小说”或“短篇小说”为准；分别在长篇或短篇项目根目录下创建。
2. 操作既有项目时，先在工作区定位项目；项目位于 `小说` 下即为长篇，位于 `短篇` 下即为短篇。
3. 用户未说明新项目的长短篇类型，或无法定位既有项目时，先询问，不得猜测。
4. 选定分支后，完整读取对应分支的 `SKILL.md`，后续只遵守该分支规则。

| 项目类型 | 必读入口 |
|---|---|
| 长篇 | `long-form/SKILL.md` |
| 短篇 | `short-form/SKILL.md` |

## 全局约束

- 在 `D:\ai小说` 搜索文件或文本时，只使用 PowerShell 原生命令，例如 `Get-ChildItem`、`Select-String` 和 `Where-Object`；不使用 `rg`、`ripgrep` 或 `grep`。
- 根入口只负责分流，不得将长篇规则套用到短篇项目，或反向套用。
