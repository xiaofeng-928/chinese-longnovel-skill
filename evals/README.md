# MyNovel evals

四个用例分别测量：必须自然化、无需自然化、自然化破坏阻断、已有扫榜证据的开书流程。

硬断言由 `evals/grade_outputs.py` 检查；自然程度和是否过度编辑仍需要在 Skill Creator 静态评审页中人工判断。旧版本基线使用 Git tag `v2.0`，不得用当前工作树伪装基线。

“需要改写”与“无需改写”分开计分，避免一个永远跳过自然化的实现靠事实保真获得高分；破坏样本必须零容忍阻断。
